# VEYA — Decision Log

**כל החלטה משמעותית** במערכת נכנסת לכאן, כדי שגם בעוד שנה נבין למה. פורמט:

```
### YYYY-MM-DD — כותרת ההחלטה
- **החלטה:** מה הוחלט.
- **סיבה:** למה.
- **השפעה:** על מה זה משפיע (מוצר/קוד/מותג/עסק).
- **חלופות שנשקלו:** מה נבחן ונדחה, ולמה.
```

הוסף רשומה חדשה **בראש** רשימת ההיסטוריה. החלטות המסומנות 🔒 הן **נעולות** —
אין לשנות בלי אישור מפורש של המייסד.

---

## החלטות נעולות (🔒)

### 🔒 Event-first architecture — VEYA היא מערכת אירועים, לא מערכת חתונות שהתרחבה
- **החלטה:** **VEYA אינה מערכת חתונות שהתרחבה לאירועים נוספים. VEYA היא
  מערכת חכמה אחת לניהול אירועים מכל סוג.** המשתמש יוצר אירוע, בוחר סוג, והמערכת
  מתאימה את עצמה אליו. **כל סוג אירוע משתמש באותה מערכת ומתאים את עצמו דרך
  `event_type`, ה-Lexicon ומנוע המונחים** — לא בונים מערכות נפרדות לכל סוג.
  זו החלטה על **זהות המוצר**, לא רק על הארכיטקטורה הטכנית: חתונה היא **מקרה
  פרטי** בתוך מערכת אירועים רחבה — העשיר והמדויק ביותר, אבל לא ההגדרה של המוצר.
  זה **מחדד ומחליף** את הניסוח "Wedding-first, Event-ready" (ההחלטה למטה) —
  לא סותר אותו: חתונה עדיין נשארת ברירת המחדל ואחת החוויות הכי עשירות
  ומדויקות במערכת, אבל לא "הבסיס שהכול נבנה עליו כתוספת".
- **סיבה:** הוראת המייסד מפורשת (2026-07-22). "Wedding-first, Event-ready"
  תיאר נכון את ה**מצב** (מה בנוי), אבל לא ביטא את ה**זהות** של המוצר בצורה
  שמגינה על החלטות עתידיות — קל היה לטעות ולבנות תכונות "לחתונה, ואולי גם
  לאחרים" במקום "לאירוע, עם ניואנס לפי סוג".
- **השפעה:** משפט העוגן ב-`SKILL.md` וב-`vision.md` שוכתב. הלקסיקון עצמו
  (`eventTypes.ts`/`event_terms.py`) עומק ל-8 סוגי האירוע — נוספו `guestsLabel`
  ("מוזמנים"/"משתתפים") ו-`giftLabel` (מתנה לפי סוג), ותוקנו מונחי בר/בת מצווה
  ("החוגג"/"החוגגת" במקום "בעל השמחה", "צד משפחת האב/האם" במקום "צד האב/האם")
  וברית (אותו תיקון) ועסקי ("שם האירוע / החברה"). ניווט האפליקציה הראשי
  (`App.tsx`) הפך דינמי — כותרת/תפריט "מוזמנים" הופכים ל"משתתפים" באירוע עסקי.
- **מה זה לא אומר:** לא "לדלל" את חוויית החתונה לטובת ניטרליות. מאגר 75
  התבניות הייעודיות לחתונה נשאר כפי שהוא. "Event-first" הוא עיקרון
  **ארכיטקטוני וזהותי**, לא ציווי לגנריות בכל טקסט.
- **חלופות שנשקלו:** להשאיר את "Wedding-first, Event-ready" כניסוח יחיד
  (נדחה — המייסד ביקש במפורש לחדד: המוצר הוא "מערכת אירועים", לא "מערכת
  חתונות שגדלה"); לבנות Workspace/מוצר נפרד לכל סוג אירוע (נדחה מפורשות —
  "לא לבנות מערכות נפרדות לכל סוג אירוע").

### 🔒 מבחן החובה לכל פיתוח: "האם זה מתאים לכל סוגי האירועים?"
- **החלטה:** VEYA היא Event First. חתונה היא סוג אירוע אחד מתוך מערכת רחבה.
  **אין לבנות פיצ'ר חדש שמניח חתונה בלבד.** כל פיתוח חדש — מסך, הודעה,
  placeholder, tooltip, empty state, validation/success/error message,
  אוטומציה, עמוד שיווקי — **חייב לשאול לפני כתיבת קוד: "האם זה מתאים לכל
  סוגי האירועים?"** אם התשובה "לא" — הפתרון עובר דרך הלקסיקון
  (`event_type` + `eventTypes.ts`/`event_terms.py`), לא טקסט/לוגיקה קשיחה
  שמניחה חתונה בשקט.
- **סיבה:** הוראת המייסד מפורשת (2026-07-22, סבב Audit שלישי) — לעגן את
  המבחן כשאלת-חובה מוקדמת, לא רק כעיקרון כללי, כדי שהוא ייבדק **בכל**
  משימה עתידית ולא רק כשמישהו נזכר לבדוק אחרי המעשה.
- **השפעה:** נוסף כ"כלל ברזל" מפורש ב-`SKILL.md` §6. סבב Audit שלישי מצא
  ותיקן דוגמאות קונקרטיות למה שקורה בלי המבחן הזה: מנוע ה"מילוי החכם"
  בעורך המפה (`seatingAdvisor.ts`) היה עיוור ל-`event_type` (משקלי ניקוד
  קבועים) עד שתוקן; תגית "מוזמנים" הופיעה קשיחה בלשונית ההושבה, באשף
  השליחה הראשון ובכמה הודעות/empty-states בעורך המפה, למרות שהניווט הראשי
  כבר היה דינמי — כלומר "Event-first חלקי" יכול להיראות כמו "Event-first
  מלא" אם לא בודקים כל מסך בנפרד.
- **חלופות שנשקלו:** להסתפק בעיקרון הכללי הקיים בלי שאלת-חובה מפורשת
  (נדחה — הוכח בפועל שזה לא הספיק: 3 סבבי Audit ומצאו כל פעם דליפות
  חדשות באזורים שלא נבדקו במפורש).

### 🔒 Wedding-first, Event-ready — VEYA מתאימה לכל אירוע בלי לפגוע בחתונה
> **מוחלף בחלקו** על ידי ההחלטה למעלה (Event-first architecture, 2026-07-22) —
> נשאר כאן כתיעוד היסטורי מדויק של מה שנבנה ולמה. הפרטים הטכניים (הלקסיקון,
> הדקדוק, המשכיות מאגר החתונה) עדיין תקפים במלואם.
- **החלטה:** VEYA נשארת **Wedding-first** (חתונה = ליבה, קהל ראשי, מילת מפתח #1),
  אבל השפה, החוויה והארכיטקטורה **Event-ready** — מתאימות לכל סוג אירוע
  (`wedding`, `henna`, `bar_mitzvah`, `bat_mitzvah`, `brit`, `family`, `business`,
  `other`). **מבחן זהב לכל שינוי:** "האם זה מגמיש לכל האירועים בלי לפגוע בחוויית
  החתונה?" חתונה תמיד ברירת המחדל (`event_type='wedding'`).
- **סיבה:** אותו מנוע ניהול-מוזמנים + RSVP + הושבה משרת כל שמחה. הרחבת שוק
  אדירה (בר/בת מצווה, ברית, אירועים) בלי לבנות מוצר חדש — רק שכבת שפה דינמית.
- **השפעה:** מנוע מונחים דינמי בשני צדדים: `frontend/src/strings/eventTypes.ts`
  ו-`backend/app/event_terms.py` (מקור אמת יחיד לשפה לפי `event_type`). שדה
  `event_type` על האירוע (ברירת מחדל `wedding`, מיגרציה בטוחה דרך `_EXTRA_COLUMNS`).
  מסכי המערכת, שכבת ההודעות/RSVP, ומאגר ההודעות נשאבים דינמית. דף הנחיתה וה-SEO
  מרחיבים ל"חתונה וכל אירוע" בלי keyword stuffing. הוספת סוג אירוע = רשומה אחת
  בכל מנוע, בלי לגעת במסכים.
- **דקדוק עברי (החלטת מפתח):** האות ה"א הידיעה נבלעת אחרי ל/ב ("להחתונה" שגוי).
  לכן `celebration` הוא **סתמי** ("חתונה"/"אירוע", בטוח אחרי ל/ב) ו-
  `celebration_construct` הוא **צורת סמיכות** ("חתונת"/"בר המצווה של"/"אירוע של",
  בלי ה"א מובילה). כך חתונות מרונדרות **זהה למקור** ושאר האירועים מתאימים נכון.
- **המשכיות:** מאגר ההודעות של החתונה (75 תבניות) **לא נגרע** — נשמר במלואו;
  אירועים לא-חתונתיים מקבלים סט GENERIC מסונן (12 תבניות מבוססות-טוקנים).
- **חלופות שנשקלו:** להישאר חתונה-בלבד (נדחה — פספוס שוק ענק); מוצר נפרד לכל
  אירוע (נדחה — כפילות אדירה); לטקן-מחדש את כל 75 תבניות החתונה (נדחה — ניסוח
  עברי מובלע-מילות-יחס שלא מיטוקן נקי; הועדף להשאיר חתונה שלמה + סט גנרי מקביל).

### 🔒 מנוע שיבוץ דטרמיניסטי — LLM לא מחשב הושבה
- **החלטה:** חישוב השיבוץ נשאר אלגוריתם דטרמיניסטי (Greedy + Local Search,
  pure-Python, בלי solver חיצוני). LLM רק לפרשנות טקסט ולשיח.
- **סיבה:** חוקים קשיחים (קיבולת, זוגות אסורים) לעולם לא ייכשלו בגלל "הזיה"
  של מודל שפה. זה ההבדל בין מנוע חכם למנוע מסוכן מבחינת אמון.
- **השפעה:** ארכיטקטורה (`seating-engine.md`), אמינות מוצרית, גבול ה-AI.
- **חלופות שנשקלו:** LLM שמחשב שיבוץ (נדחה — לא אמין לחוקים קשיחים);
  OR-Tools (לא היה זמין, וגם לא נדרש — pure-Python עבד מצוין).

### 🔒 ערוץ מוזמנים = WhatsApp Business API, כפתורים לא טקסט חופשי
- **החלטה:** RSVP דרך Meta Cloud API עם Quick Reply Buttons.
- **סיבה:** אחוזי פתיחה/מענה גבוהים בישראל; כפתורים = מהירות ומניעת פרשנות שגויה.
- **השפעה:** אינטגרציה, חוויית מוזמן, מגבלת 3 כפתורים / List Message.
- **חלופות שנשקלו:** SMS/אימייל (נדחו — מענה נמוך); טקסט חופשי (נדחה — פרשנות).

### 🔒 תמחור חד-פעמי לפי אירוע (לא מנוי)
- **החלטה:** תשלום חד-פעמי לכל חתונה.
- **סיבה:** חתונה היא אירוע חד-פעמי; מנוי חודשי ירגיש לא רלוונטי ויפגע בהמרה.
- **השפעה:** מודל עסקי, תקשורת שיווקית.
- **חלופות שנשקלו:** מנוי (נדחה — לא תואם אופי הצריכה).

### 🔒 לולאת הבהרות לפרסור מגבלות (שאלה סגורה, לא חוסמת)
- **החלטה:** כשפרסור הערה עמום — שאלה סגורה בכפתורים, בתור "הבהרות ממתינות".
- **סיבה:** שם פרטי בלבד לא ניתן לשיוך חד-משמעי; שאלה סגורה מהירה ולא חוסמת זרימה.
- **השפעה:** UX, מנגנון ה-AI, WhatsApp (3 כפתורים / List).
- **חלופות שנשקלו:** שאלה פתוחה (נדחתה); מודל חוסם באמצע כתיבה (נדחה).

### 🔒 מתנות באשראי = עמוד מרכזי בחזון, לא פיצ'ר צדדי
- **החלטה:** מתנות באשראי הן עמוד ליבה ב-Wedding OS (`roadmap.md`).
- **סיבה:** מקור הכנסה, בידול, הארכת מחזור החיים של המשתמש עד אחרי החתונה.
- **השפעה:** רודמאפ, אסטרטגיה עסקית, דרישות אבטחה/סליקה.
- **חלופות שנשקלו:** להשאיר מתנות מחוץ למוצר (נדחה — פספוס ערך מרכזי).

---

## היסטוריית החלטות (החדש בראש)

### 2026-08-09 — תיקון תצוגה: מסך אדמין "ספריית נוסחים לבחירה" הציג רק שלב אחד
- **הבאג:** הבעלים דיווח שרואה רק "הזמנה" במסך האדמין, למרות שכל 6 השלבים
  מלאים ב-DB (ראו הרשומה הבאה). הסיבה: `MessageDefaultOptionsManager`
  סינן לפי שני dropdown-ים (סוג אירוע + סוג הודעה), וברירת המחדל של סוג
  ההודעה הייתה "הזמנה" — כדי לראות שלב אחר היה צריך לבחור אותו ידנית,
  לא היה ברור שזה בכלל אפשרי. **התוכן עצמו היה תקין ומלא** — זה היה באג
  תצוגה בלבד, לא נתונים חסרים (המסך של הזוג, `CommunicationTab`, הציג נכון
  את כל 6 השלבים כל הזמן — נבדק ואומת).
- **התיקון:** dropdown סוג-ההודעה הוסר לגמרי. נשאר רק בורר סוג-אירוע;
  מתחתיו מוצגים **כל 6 השלבים יחד**, כל אחד בכותרת נפרדת + עד 12 הכרטיסים
  שלו + כפתור "הוספת נוסח" משלו. Backend: `GET /admin/message-default-options`
  כבר תמך ב-`message_type` אופציונלי (לא שונה); רק ה-API call בפרונט
  הפסיק לשלוח אותו. לא נגעו בתוכן/בנתונים כלל — הוראה מפורשת של הבעלים.
- **החלטה/עובדה:** הבעלים השלים לשלוח את כל 6 מקבצי הנוסחים לחתונה
  (הזמנה, תזכורת ראשונה/שנייה/אחרונה, יום האירוע, תודה — 12 כל אחד).
  כולם הוזנו ל-`MessageDefaultOption` (טוקנים הומרו ל-`{{...}}`, כל אחד
  תויג ב-tone קצר), ואומתו ברינדור אמיתי מול אירוע+מוזמן לדוגמה. לפי
  הנחיית הבעלים ("אל תעבור לאירועים אחרים לפני שחתונה שלמה") — עכשיו,
  ורק עכשיו, אפשר לעבור לסוג האירוע הבא כשהבעלים ישלח נוסחים עבורו.
- **ממצא לתשומת לב הבעלים (לא תוקן, לא בהיקף המשימה):** 12 נוסחי "יום
  האירוע" כולם מזכירים "מתנה באשראי" — פיצ'ר שלא נבנה עדיין, אז המשפט
  המזמין נשאר בלי קישור אחריו ברינדור בפועל. תועד ב-`open-questions.md`
  #18א ו-`product-state.md`.
- **עדיין פתוח:** ה"ברירת מחדל" האוטומטית (`MessageDefault`, מה שמוקצה
  לאירוע חדש לפני שהזוג בוחר בעצמו) עדיין מכילה רק שורה אחת עם תוכן
  (הזמנה) מתוך 48 — שכבה נפרדת מספריית הבחירה, לא הושלמה באותו סבב.
  וכמובן: **הכול עדיין בסביבת הפיתוח המקומית בלבד, לא בייצור.**

---

### 2026-08-06 — ספריית נוסחים לבחירה (MessageDefaultOption) — חזרה חלקית מ"נוסח קבוע יחיד", חתונה בלבד
- **החלטה:** הבעלים ביקש במפורש לבטל את החלטת "נוסח סופי אחד לכל שלב" (שתי
  הודעות קודם באותה שיחה!) ולחזור למודל שבו **הזוג עצמו בוחר** מתוך עד 12
  נוסחים לכל event_type×message_type — אך **לא** חזרה למבנה הישן של
  `message_library.py` (קטגוריות/סגנונות פתוחים לחלוטין): נשאר קיבוע קשיח
  ל-6 סוגי הודעה (`reminder_3`/`after_event_thank_you` שהבעלים כתב הם רק
  שמות חלופיים ל-`final_reminder`/`thank_you` הקיימים — **לא** נוספו שלבים
  חדשים, אושר מפורשות בשאלת הבהרה).
  נוסף מודל חדש **אדיטיבי בלבד**: `MessageDefaultOption`
  (`event_type, message_type, option_number 1-12, tone, title, content,
  variables_supported, is_active`) — **לא** מחליף את `MessageDefault`
  הקיים (שנשאר "מה שמוקצה אוטומטית לאירוע חדש", ללא שינוי בהתנהגות). הזוג
  בוחר וריאציה דרך `GET /communication/sequence/{type}/options` (חדש,
  read-only, מסונן ל-`is_active` ותוכן לא ריק) בתוך `CommunicationTab` —
  כפתור "נוסחים מוכנים לבחירה" שפותח רשימת אופציות מתויגות ב-tone קצר;
  בחירה מעתיקה את התוכן לטקסטרה המקומית (עדיין דורש "שמירה" מפורש — לא
  שינוי אוטומטי, לא נשבר שום דבר בזרימת השמירה/עריכה הקיימת).
  לאדמין נוסף `MessageDefaultOptionsManager` (`AdminPage.tsx`) — בורר
  event_type×message_type + כרטיסי עריכה **+ הוספה/מחיקה** (עד 12), לפי
  דרישת הבעלים המפורשת "שמור אותם במקום שהאדמין יכול לערוך ולהוסיף — לא
  טקסט קשיח בקוד".
  **נזרעו בפועל (חתונה בלבד, 72 שורות):** 12 נוסחי הזמנה אמיתיים (אותם 12
  שהבעלים שלח, טוקנים הומרו ל-`{{...}}`, כל אחד עם תגית tone קצרה שאני
  כתבתי — למשל "חם ומלא פרטים"/"אישי ומשתף"/"נרגש וחגיגי" — לא תוכן חדש,
  רק תיוג של תוכן קיים) + 60 שורות ריקות (12 לכל אחד מ-5 השלבים הנותרים:
  reminder_1/reminder_2/final_reminder/event_day/thank_you), ממתינות
  לנוסחים שהבעלים שלח שישלח בהמשך (**הוא כותב אותם, לא אני** — אושר
  מפורשות בשאלת הבהרה השלישית). **שום שורה לא נוצרה לשאר 7 סוגי האירוע** —
  אושר מפורשות "אל תעבור לאירועים אחרים לפני שחתונה שלמה".
  הזריעה רצה מ-`main.py: seed_message_default_options()` בעליית השרת (כמו
  `seed_message_defaults()` הקיים) — "רק אם הטבלה ריקה", כך שתרוץ אוטומטית
  גם בייצור בפריסה הבאה (לא נדרש סקריפט ידני נפרד).
- **סיבה:** הבעלים הבהיר בשאלות ההבהרה: (1) הזוג בוחר מתוך 12 בפועל
  ("פיצ'ר חדש למשתמש קצה", לא רק ארכיון גיבוי לאדמין) — (2) reminder_3/
  after_event_thank_you הם שינוי שם בלבד, לא שלב שביעי — (3) הבעלים עצמו
  ישלח את 60 הנוסחים הנותרים, אני רק בונה את התשתית שמקבלת אותם. "אל תמציא
  תוכן" (עיקרון קבוע מהסבב הראשון) עדיין בתוקף לגבי 60 השורות הריקות; לגבי
  ה-12 שכבר נשלחו, הבעלים ביקש מפורשות שאבחר/אארגן אותם (ראו הרשומה
  הקודמת) — תיוג ה-tone הוא ארגון של תוכן קיים, לא כתיבת תוכן חדש.
- **השפעה:** ⚠️ שוב — כל זה קיים רק ב-DB המקומי של הפיתוח, לא בייצור.
  ברגע שהקוד הזה נפרס ל-Render, `seed_message_default_options()` ירוץ
  אוטומטית ויזרע את אותן 72 שורות שם (הטבלה ריקה בייצור). נבדק קצה-לקצה
  בדפדפן: הזוג רואה את 12 האופציות (רק להזמנה, שאר השלבים מוסתרים כי
  תוכנם ריק), בחירה מעתיקה נכון לעורך; האדמין רואה את כל 12 המשבצות של כל
  שלב (גם ריקות), עריכה/הוספה/מחיקה עובדות, מגבלת 12 נאכפת.
- **חלופות שנשקלו:** להשאיר נוסח קבוע אחד ולתת לזוג רק "השראה" בלי לבחור
  בפועל — נדחה, הבעלים ביקש בחירה אמיתית. להרחיב מחדש את `message_library.py`
  הישן (קטגוריות פתוחות) — נדחה, שונה ממה שהבעלים ביקש (6 שלבים קבועים,
  לא ספרייה פתוחה).

---

### 2026-08-06 — נוסח חתונה/הזמנה ראשון הוזן + הרחבת רשימת המשתנים ל-groom_name/bride_name
- **החלטה:** הבעלים שלח 12 נוסחי הזמנה לחתונה (כולם באיכות טובה, בפורמט
  טוקן ישן `{שם אורח}` וכו') וביקש שאבחר בעצמי אחד מהם כנוסח הסופי, במקום
  לשמור את כל 12 כאפשרויות בחירה (זה היה בדיוק המנגנון שהוסר בסבב הקודם).
  נבחר **אופציה 1** — הכי תמציתית מבין המלאות (כוללת גם קישור אישור הגעה
  וגם קישור ניווט, ושני השמות), בלי חזרות מיותרות. הטוקנים הומרו לפורמט
  `{{...}}` הקיים. שורת `MessageDefault(event_type='wedding',
  message_type='invitation').content` עודכנה בפועל (ב-DB המקומי של הפיתוח
  בלבד — ראו "השפעה" למטה).
  בנוסף: הטקסטים הפרידו בפירוש בין `{שם החתן}` ל־`{שם הכלה}`, בעוד המערכת
  תמכה רק במשתנה מאוחד `{{host_names}}` (עקרון Event-first — לא כל סוג
  אירוע יש לו שני מארחים). הבעלים אישר במפורש הוספת שני משתנים חדשים,
  **ייעודיים לחתונה בלבד**: `{{groom_name}}` ו-`{{bride_name}}`
  (`communication.py: WEDDING_ONLY_VARIABLE_KEYS`, מחושבים תמיד ב-
  `communication_values()` מ-`event.groom_name`/`event.bride_name` — לא
  משפיע על סוגי אירוע אחרים כי הם פשוט לא מוצעים/משמשים שם). גם הוספנו
  `variables_supported` ל-`schemas.MessageDefaultUpdate` (היה חסר — אי אפשר
  היה לעדכן את השדה הזה דרך ה-API בכלל), כדי שאפשר יהיה לתעד אילו משתנים
  רלוונטיים לנוסח הספציפי הזה.
- **סיבה:** "אל תמציא תוכן" נשאר בתוקף, אבל משעה שהבעלים בעצמו סיפק את
  התוכן וביקש מפורשות שאני אכריע — זו כבר לא המצאה אלא ביצוע החלטה שהואצלה
  אליי. Wedding-first, Event-ready: העושר של "שם החתן"/"שם הכלה" בנפרד הוא
  בדיוק המקרה הפרטי העשיר שחתונה אמורה לקבל, בלי לדרוס את `{{host_names}}`
  הכללי (עדיין קיים ותקף לכל הסוגים, כולל חתונה עצמה אם רוצים ניסוח מאוחד).
- **השפעה:** ⚠️ **הנוסח הוזן רק ב-DB המקומי של הפיתוח (SQLite), לא בייצור**
  (Supabase/Postgres דרך Render) — לא בוצעה כאן שום פעולה מול סביבת הייצור.
  כדי שהזוגות האמיתיים יראו את הנוסח הזה, על הבעלים להיכנס למסך האדמין
  החי (`/admin/message-defaults`) ולהדביק את אותו הטקסט לשורת
  חתונה/הזמנה, או לבקש זאת מפורשות בהמשך. 11 האופציות שלא נבחרו לא נשמרו
  באפליקציה — רק בהודעה המקורית של הבעלים.
- **חלופות שנשקלו:** לשמור את כל 12 כרשימת בחירה לזוג — נדחה מפורשות
  (חוזר בדיוק לספרייה המורכבת שהוסרה). להשתמש רק ב-`{{host_names}}` בלי
  להוסיף משתנים חדשים — נדחה, כי הבעלים ביקש בפירוש את ההפרדה.

---

### 2026-08-05 — "ספריית הודעות מוכנות" — תצוגה חדשה, לקריאה בלבד, מעל אותה תשתית
- **החלטה:** נוסף מסך/מודל חדש "ספריית הודעות מוכנות" — **לא** מנגנון נוסף
  ולא ריפקטור של "תקשורת עם אורחים" (ראו הרשומה הבאה למטה), אלא שכבת תצוגה
  read-only מעל אותו `MessageDefault` שכבר קיים: בורר סוג אירוע (chips, כל
  8 הסוגים דרך `EVENT_TYPE_OPTIONS`, ברירת מחדל = סוג האירוע הנוכחי) ומתחתיו
  6 כרטיסי הודעה (`title`+`content`) בסדר הקבוע, לחיצה על כרטיס פותחת/סוגרת
  תצוגה מלאה. אין עריכה, אין שמירה, אין תצוגה מקדימה מרונדרת (מציג את
  `content` הגולמי כולל `{{variables}}`, כדי לא לשכפל את מנגנון ה-preview
  הקיים ב-`CommunicationTab`/`/communication/sequence/{type}/preview`).
  Backend: `GET /communication/library` חדש — קריאה בלבד, `get_current_user`
  (לא `EventAccess`, כי הנתון גלובלי ולא תלוי-אירוע; לא `get_current_admin`,
  כי זה מיועד לזוג עצמו לא רק לאדמין). Frontend: `MessageLibrary.tsx` חדש,
  כניסה משני מקומות — כפתור מעל חוויית הזוג ב-`CoupleRsvpView` (מודל,
  overlay משותף עם `SendInvitationsDialog`) + לשונית שלישית בפאנל הטכני
  (`AdminRsvpView`). לא נגעו: `CommunicationTab.tsx`, `/communication/sequence/*`,
  `/admin/message-defaults` (מסך הזנת התוכן של האדמין) — כולם נשארו בדיוק
  כפי שהיו.
- **סיבה:** הוראת הבעלים המפורשת בסבב הזה: "בשלב הזה אין לבצע שינוי במערכת
  ההודעות הקיימת ואין לבצע ריפקטור... רק להוסיף ספרייה לקריאה". הבעלים
  ישלח את נוסחי ההודעות הסופיים בהמשך; עד אז כל הכרטיסים מציגים placeholder
  ריק (העקרון "אל תמציא תוכן" מהסבב הקודם עדיין בתוקף).
- **השפעה:** ניתן לעיין בכל 48 ברירות המחדל (8 סוגי אירוע) מתוך אזור
  האירוע, בלי לצאת לפאנל אדמין ובלי סיכון לעריכה בטעות. ברגע שהבעלים ישלח
  את הטקסטים ויוזנו דרך `/admin/message-defaults`, הם יופיעו כאן אוטומטית.
- **חלופות שנשקלו:** להרחיב את `MessageDefaultsManager` הקיים (אדמין) כך
  שגם הזוג ייגש אליו — נדחה: זה מסך טכני-לעריכה, לא מתאים לחוויית זוג לא-טכני.
  לבנות preview מרונדר לכל הודעה — נדחה בשלב זה: מכפיל לוגיקה קיימת ומטרת
  המסך היא עיון בנוסח הגולמי, לא הדמיה מדויקת (זו כבר קיימת ב"תקשורת עם
  אורחים").

---

### 2026-08-05 — איחוד 3 מנגנוני ההודעות לרצף קבוע אחד ("תקשורת עם אורחים") — שלב 1: תשתית בלבד
- **החלטה:** שלושת המנגנונים שהיו קיימים במקביל — (1) "ספריית ההודעות"
  הישנה (`message_library.py`, ~2100 שורות, קטגוריה×סגנון×סוג אירוע +
  `MessageBuilder.tsx`), (2) מנוע אוטומציות חופשי-לגמרי
  (`AutomationRule`+`MessageTemplate` + `AutomationRulesTab`/
  `AutomationTemplatesTab`/`AutomationQueueTab`, שהזוג הגדיר בעצמו
  טריגר+דיליי+קהל+תבנית), ו-(3) "מסלול RSVP" (`VeyaTemplate`+
  `VeyaWorkflowStep` + `rsvp_track.py`, ברירות מחדל גלובליות מועתקות
  לכל אירוע) — אוחדו למודל **אחד, פשוט וקבוע**: לכל `event_type` בדיוק 6
  סוגי הודעה (`invitation`/`reminder_1`/`reminder_2`/`final_reminder`/
  `event_day`/`thank_you`), ללא קטגוריות/סגנונות לבחירה. מודלים חדשים:
  `MessageDefault` (קטלוג גלובלי event_type×message_type, 48 שורות,
  `content=""`) ו-`EventMessage` (עותק בפועל לכל אירוע, idempotent,
  `backend/app/communication.py: provision_event_messages`). הוקצה
  Backfill לכל האירועים הקיימים (לא רק חדשים). ה-UX הישן (4 לשוניות
  בפאנל האדמין הטכני + `MessageBuilder` בתוך מסך הזוג) הוחלף במסך אחד
  "תקשורת עם אורחים" (`CommunicationTab.tsx`) — 6 כרטיסים קבועים, כל אחד
  עם עריכה/הפעלה-כיבוי/תצוגה מקדימה/שליחה לבדיקה. **שלב 1 בלבד: אין תוכן.**
  כל `content` נשאר `""` — הבעלים יזין את הטקסטים הסופיים דרך מסך אדמין
  חדש (`/admin/message-defaults`, `MessageDefaultsManager` ב-`AdminPage.tsx`).
- **סיבה:** הוראת הבעלים המפורשת — שלושת המנגנונים יצרו חוויית שימוש
  מבולבלת (איפה עורכים הודעה? בספרייה? בתבניות? בחוקים?). בשאלת הבהרה
  שנשאלה במפורש, הבעלים אישר **איחוד מלא** (לא רק ניקוי הספרייה) ו-**Backfill
  לכל האירועים הקיימים** (לא רק חדשים).
- **השפעה:** Backend — `models.py` (MessageDefault/EventMessage חדשים;
  `MessageTemplate`/`AutomationRule`/`VeyaTemplate`/`VeyaWorkflowStep`
  מסומנים DEPRECATED בקוד, **נשארים בקובץ** ולא נמחקים — אין Alembic,
  ו-`Message.rule_id` הישן מצביע עליהם; `Message` קיבל עמודה חדשה
  `event_message_id`), `communication.py` חדש (רינדור `{{var}}` חדש —
  12 המשתנים המדויקים שהוגדרו, שונה מהטוקנים הישנים בסוגריים מרובעים),
  `routers/communication.py` חדש, `routers/automation.py` נכתב מחדש
  (הוסרו `/library`, `/templates` CRUD, `/rules` CRUD, `/due`, `/run-due`;
  **נשארו** `/track/*`/`/timeline*`/`/dashboard` — אלה מזינים את `DashboardTab`
  הקיים במסך הזוג, ורק הוחלף המקור הפנימי שלהם מ-`rsvp_track.py`/
  `AutomationRule` ל-`EventMessage`/`communication.py`, כדי לא לשבור את
  זרימת השליחה האמיתית שכבר עובדת), `routers/admin.py` (הוסר ניהול
  VeyaTemplate/Workflow, נוסף `/admin/message-defaults` + `/backfill`),
  `message_library.py`+`rsvp_track.py` **נמחקו לגמרי**. Frontend — נמחקו
  `MessageBuilder.tsx`/`AutomationRulesTab.tsx`/`AutomationTemplatesTab.tsx`/
  `AutomationQueueTab.tsx`; נוסף `CommunicationTab.tsx`; `RsvpPage.tsx`
  איבד את הלשוניות automations/templates/queue/manual לטובת לשונית
  `communication` יחידה; `AdminPage.tsx` קיבל `MessageDefaultsManager`.
  כל הבדיקות הקיימות (backend + frontend) ממשיכות לעבור ללא regressions.
- **חלופות שנשקלו:** לנקות רק את (1) ולהשאיר (2)+(3) גמישים (נדחה —
  הבעלים ביקש איחוד מלא במפורש, לא רק ניקוי קוסמטי); למחוק גם את הטבלאות
  הישנות מה-DB (נדחה — אין Alembic, מחיקת טבלה/עמודה בלי מיגרציה אמיתית
  מסוכנת; הטבלאות נשארות ריקות/לא-נכתבות, לא נמחקות); Backfill רק לאירועים
  חדשים (נדחה — הבעלים אישר מפורשות Backfill לכל האירועים).
- **המשך ידוע (לא בשלב הזה):** `routers/messaging.py: send_invitations/
  send_reminders` (הפלואו הישן שהיה משמש את `ManualTab` שנמחק) עדיין קיימים
  ולא חוברו למקור החדש — ללא קורא ב-Frontend כרגע, הם קוד מת בטוח אך לא
  נוקה. כשהבעלים ישלח את הטקסטים הסופיים, שלב 2 יטמיע אותם ב-`MessageDefault`
  ואפשר יהיה לשקול חיווט/הסרה סופית של הנתיב הישן.

### 2026-08-05 — כרטיס "סטטוס אישורי הגעה" הפך למד חצי-עגול (Gauge), לא דונאט
- **החלטה:** הדונאט המלא בדשבורד (הכרטיס "סטטוס אישורי הגעה") הוחלף במד
  חצי-עגול (180°) בהשראת לוח מחוונים של רכב יוקרה — קשת מונפשת (ציור
  משמאל לימין + זוהר רך), מספר-אחוז ענק במרכז (Cormorant Garamond, יוצא
  דופן מכוונה מהמוסכם "הכול Heebo" בדשבורד כי זה עכשיו המספר-הגיבור),
  וארבעה כרטיסי סטטוס (אישרו/מתלבטים/לא אישרו/ממתינים) מתחתיו. **מחליף
  את פלטת ה-12-ההצעות הקודמת** (טוקנים `--donut-*`) בפלטה חדשה
  (`--gauge-*`): ירוק #34c759, צהוב-זהב `--gold-light`, אדום רך #d96c63,
  אפור ניטרלי #cbcbd1 — לפי הנחיה מפורשת ומפורטת של המייסד.
- **סיבה:** הוראת המייסד (בקשת עיצוב מלאה, "Lead Product Designer") —
  הדונאט המלא לא הרגיש כמו נקודת המיקוד הרגשית של הדשבורד. מד חצי-עגול
  עם אנימציה איטית (~1 שנייה) ומספר עולה נועד להעביר תחושת "כמעט מוכנים
  לחתונה" ברגע הראשון שנפתח הדשבורד.
- **השפעה:** `DashboardPage.tsx` (קומפוננטת `RsvpGauge` חדשה מחליפה את
  `Donut`; `useCountUp`/`describeArc`/`polarToCartesian` חדשים), `App.css`
  (טוקני `--gauge-*`, מחלקות `.gauge-*`, הוסרו `.donut-wrap/.donut/.donut-bg/
  .donut-center/.donut-num/.donut-lbl/.rsvp-center-donut/.rsvp-summary/
  .donut-legend-row/-item/-dot/-count` — כולן היו בשימוש בדשבורד בלבד),
  `strings/he.ts` (נוספו `gaugeLabel`/`gaugeStatusMaybe`/`gaugeStatusDeclined`,
  הוסרו `donutCenterLabel`/`donutCenterTotal`/`segMaybe` שהתייתרו).
  **הערה טכנית ל-RTL:** ל-SVG של המד נדרש `direction: ltr` מפורש — בלעדיו
  דפדפנים משקפים (mirror) קשתות SVG בתוך עמוד RTL, וסדר הצבעים יוצא הפוך.
- **חלופות שנשקלו:** להשאיר את הדונאט המלא ורק לשדרג צבעים/אנימציה
  (נדחה — המייסד ביקש שינוי קומפוננטה מלא, לא שיפור הדרגתי); Tailwind
  לקומפוננטה הזאת בלבד כפי שהתבקש במפורש (נדחה — בפרויקט אין Tailwind
  מותקן כלל, והוספת מערכת עיצוב שנייה לרכיב יחיד היא סיכון ארכיטקטוני
  לא פרופורציונלי; הושג אותו תוצר חזותי עם CSS רגיל תואם למוסכמות הקובץ).
- **עדכון (אותו יום):** המד הוצא מתוך כרטיס Grid קטן (חלק מ-`.dash-grid-2col`
  לצד "הצעד הבא") לסקשן עצמאי במלוא הרוחב (`.gauge-section`) מיד מתחת
  להירו — "חלון ראווה" בלי כרטיסים אחרים לצידו. "הצעד הבא" עבר לשורה
  נפרדת משלו (`.dash-next-row`) מתחת לסקשן המד. גודל המד/המספר עכשיו
  יחסי (`%`/`clamp()`) במקום px קבוע, כך שהוא "מונומנטלי" בדסקטופ ונשאר
  חצי-עגול ומלא-רוחב-כמעט במובייל בלי שבירות מדיה-קוורי נוספות לטאבלט.
  `.dash-grid-2col` הוסר לגמרי (התייתר).

### 🔒 2026-08-01 — "לא לשבת ליד" = אותו שולחן בלבד
- **החלטה:** זוג "לא לשבת יחד" נחסם מ**אותו שולחן**. שולחן סמוך אינו נחסם,
  גם כשההערה נוסחה "לא לשבת ליד X".
- **סיבה:** החלטת המייסד. חסימת שולחנות סמוכים כחוק קשיח הופכת אולם צפוף
  לבלתי פתיר, והמערכת הייתה מחזירה "נמצאה התנגשות" גם כשקיים סידור סביר.
- **השפעה:** `seating.py`, `constraints.py`, `test_seating_flow.py`.
- **חלופות שנשקלו:** חסימה קשיחה גם לשולחן סמוך (נדחה — אי-פתירות);
  קנס ניקוד כבד לשולחן סמוך (נדחה כרגע — סיבוכיות מול תועלת לא מוכחת;
  אפשר לחזור לזה אם יגיע משוב אמיתי מזוגות).

### 2026-08-01 — הפרדת הערות: פנימית מול הערות הושבה
- **החלטה:** שדה חדש `Guest.seating_notes` הוא **המקור היחיד** שמנוע
  ההושבה קורא מהבעלים. `notes_raw` הפך להערה פנימית שאינה מנותחת כלל.
  `guest_note` (מה שהמוזמן כתב ב-RSVP) ו-`Event.group_notes` ממשיכים להזין
  **העדפות אזור/נגישות בלבד**.
- **סיבה:** הערה תפעולית ("צריך לחזור אליו") התפרשה כאילוץ ישיבה. הפרדה
  מפורשת עדיפה על ניסיון לנחש כוונה מטקסט חופשי.
- **השפעה:** `models.py`, `schemas.py`, `constraints.py`, `routers/{seating,
  hall,constraints,stats,auth}.py`, `importer.py`, `AddGuestForm`,
  `GuestsPage`, `types.ts`, `he.ts`.
- **חלופות שנשקלו:** להעתיק אוטומטית הערות קיימות לשדה החדש (נדחה ע"י
  המייסד — "מתחילים נקי"); במקום זה נבנה באנר שמזהה הערות שנראות כמו העדפת
  ישיבה ומציע להעביר, בהחלטה מפורשת של המשתמש. להשאיר את `guest_note` מחוץ
  למנוע (נדחה — זו בקשת נגישות של המוזמן עצמו, הסרתה רגרסיה).

### 2026-08-01 — מנוע שיבוץ אחד בלבד; מחיקת המנוע שבצד הלקוח
- **החלטה:** `computeSmartFill` ב-`seatingAdvisor.ts` נמחק. "השלמת מי שללא
  שולחן" היא `POST /seating/generate` עם `only_unassigned: true`.
- **סיבה:** היו שני מנועי ניקוד עם שתי טבלאות משקלים. זה שבצד הלקוח לא הכיר
  העדפות אזור, מיקומי שולחנות או שולחנות נעולים — ולכן נתן לאותה שאלה תשובה
  שונה. שני כפתורי "אוטומטי" שמסדרים אחרת הם מקור ישיר לחוסר אמון.
- **השפעה:** `seatingAdvisor.ts` (‎−6,277 תווים), `HallPage.tsx`,
  `schemas.py`, `routers/seating.py`.
- **חלופות שנשקלו:** לסנכרן את שתי טבלאות המשקלים (נדחה — כפילות שתסטה שוב).

### 2026-08-01 — מחיקת שכבת הדסקטופ המתה במסך האולם
- **החלטה:** ~910 שורות ב-`HallPage.tsx` ו-`SmartAssistantPanel.tsx` נמחקו.
- **סיבה:** הדגל שגידר אותן היה קבוע (`const mobileMode = true as boolean`),
  ולכן הן מעולם לא רצו. פיצ'רים שישבו רק שם היו בלתי נגישים: פאנל האילוצים
  וההבהרות, ידית סיבוב השולחן, הוספת אלמנט "כניסה", הצגת אזהרות השרת,
  הצגת שגיאות, וחלון "מצב יום האירוע" (שהכפתור אליו כן היה קיים ולא עשה כלום).
- **השפעה:** כל מה שנמנה הועבר לשכבה החיה. בחירה מרובה של שולחנות וגרירת
  מוזמן ב-HTML5 DnD ירדו — הן היו קיימות רק שם.
- **חלופות שנשקלו:** להחזיר breakpoint אמיתי ולהחיות את הדסקטופ (נדחה —
  השכבה החיה כבר מיועדת לשני הגדלים, והחייאה הייתה מכפילה תחזוקה).

### 2026-08-01 — רשת ביטחון למיגרציות סכימה
- **החלטה:** `_ensure_columns()` מוסיף אוטומטית עמודה שקיימת במודל, חסרה
  ב-DB, ואין לה רשומה ידנית ב-`_EXTRA_COLUMNS` — לפי הטיפוס שבמודל.
- **סיבה:** `events.groom_parents_line` נוספה למודל בסבב 3ח בלי רשומת
  מיגרציה, ולכן כל DB קיים נשבר בכל שאילתה על events. בלי Alembic, שכחה
  אחת מפילה את המערכת.
- **השפעה:** `main.py`, `tests/test_schema_migrations.py`.
- **חלופות שנשקלו:** להוסיף Alembic (נדחה כרגע — שינוי תשתיתי גדול מדי
  לסבב הזה; נשאר ב-`roadmap.md`); רק לתקן את שתי העמודות (נדחה — לא מונע
  הישנות).

### 2026-07-25 — סבב הקופי 3א-3ו: מאגר ההודעות שופר לרגיסטרים דתיים
- **החלטה:** סבב 6 שלבים על `message_library.py` — 3א (8 סמיכויות שבורות),
  3ב (12 ניסוחים מאשימים/לוחצים), 3ג (מיזוג 4 כפילויות + בידול 3 חוצי-שלבים +
  4 תבניות חדשות), 3ד (12 מופעי `יקר/ה` → `יקרים` + הבהרת כלל האמוג'י), 3ה
  (כתיב "חנייה", 6 אמוג'י אסורים, 6 הבטחות חנייה → "הכנה"), **3ו (טקסונומיית
  רגיסטרים דתיים אמיתית — דתי/חב"ד/חרדי, כולל שדות הורים ב-Event, +9 תבניות
  חדשות)**. מקורות אמת קופי: `veya-copy` + `research-israeli-events.md`.
- **סיבה:** לפני הסבב הספרייה הייתה חילונית-חמה בלבד: 141 תבניות שכולן פותחות
  ב"הזוג/בעלי האירוע מזמינים". המחקר מראה שבישראל **חמישה** רגיסטרים של הזמנה,
  ובשלושה מהם (דתי/חב"ד/חרדי) ההורים הם המזמינים — מוצר שלא תומך בהם מפסיד
  נתח שוק אמיתי (לא ניואנס סגנוני, שינוי במבנה המשפט).
- **השפעה:** `message_library.py` (141 → 150 · 13 סגנונות במקום 10); `models.py`
  (2 שדות חדשים ב-Event); `messaging.py` (2 טוקנים חדשים ב-AUTOMATION_PLACEHOLDERS);
  migration ידני ב-Supabase (SQL בקומיט 3ו-2/4). ה-UI (טופס יצירת/עריכת אירוע)
  עדיין ללא שדות ההורים — סבב פרונט נפרד. מדדי איכות בסוף הסבב: 0 סמיכויות
  שבורות, 0 ניסוחים מאשימים, 0 מילים אסורות, 0 אי-התאמות מגדר, 0 הבטחות
  שהמערכת לא יכולה לאמת, 0 רגרסיות.
- **חלופות שנשקלו:** להוסיף רק סגנון "דתי" מאוחד (נדחה — 3 העולמות שונים
  באמת); למחוק את `traditional` (נדחה — יש קהל אמיתי חילוני-חגיגי שרוצה
  "בשעה טובה ומוצלחת" בלי דת); Alembic למיגרציה (נדחה — הפרויקט לא משתמש
  ב-Alembic, החלטה ארכיטקטונית לסבב נפרד).

### 2026-07-25 — שדות `groom_parents_line` / `bride_parents_line` ב-Event
- **החלטה:** הוספת שני שדות `String` (nullable, ברירת מחדל `""`) למודל
  `Event` — שורת הורי החתן ושורת הורי הכלה כטקסט חופשי (למשל "משפחת כהן",
  "יצחק ורבקה כהן", "פנחס וחיה שרה שיחיו"). נחשפים בתבניות כטוקנים
  `[הורי החתן]` / `[הורי הכלה]`. ריק = fallback דטרמיניסטי (`smart-line-drop`
  ב-`messaging.py:273` מוחק את השורה, התבנית ממשיכה כתקינה עם `[שמות בני הזוג]`).
- **סיבה:** מחקר `research-israeli-events.md` §4 קובע שברגיסטרים דתי/חב"ד/
  חרדי **ההורים הם המזמינים, לא הזוג** — שינוי במבנה המשפט, לא סגנון.
  141 התבניות היום פותחות כולן ב"הזוג מזמין"; ללא שדות ההורים, שום ניסוח לא
  יגשר על הפער הזה. שדות טקסט חופשי (A1 מדוח 3ו) גמישים לכל רגיסטר בלי
  לחייב את החילוני למלא שום דבר.
- **השפעה:** `models.py` (2 שדות חדשים); `messaging.py`
  (`AUTOMATION_PLACEHOLDERS` + `build_automation_values` + `render_automation_template`
  קיבלו את הפרמטרים החדשים); מיגרציה ב-Supabase נדרשת:
  ```sql
  ALTER TABLE events
    ADD COLUMN groom_parents_line VARCHAR NOT NULL DEFAULT '',
    ADD COLUMN bride_parents_line VARCHAR NOT NULL DEFAULT '';
  ```
  שאר הקוד (routers, admin, seating, frontend) אינו נדרש לשינוי; השדות
  אופציונליים בכל הזרימות. **טופס יצירת/עריכת אירוע ב-Frontend** — סבב נפרד.
- **חלופות שנשקלו:** A2 (4 שדות מובנים — נדחה: טופס גדול, פחות מדויק
  לרגיסטרים דתיים שכותבים "משפחת X" בלבד); A3 (טוקן חופשי אחד `[מזמינים]`
  לכל תבנית — נדחה: שוחט את יתרון האוטומציה החוצה-תבניות); Alembic
  כפתרון migration (נדחה: המערכת משתמשת ב-`Base.metadata.create_all`,
  החלטה ארכיטקטונית לסבב נפרד).

### 2026-07-24 — הקמת `veya-copy`: סקיל נפרד לשפה ולתקשורת
- **החלטה:** מוח השפה של VEYA יושב בסקיל **נפרד** — `.claude/skills/veya-copy/`
  (SKILL.md + 9 קבצי `references/`), ולא בתוך `veya-master`. חלוקת האחריות:
  `veya-master` מחליט **מה** לבנות ולמה; `veya-copy` מחליט **איך זה מדבר**.
  נקבע **חוק המקור היחיד**: הסקיל מסביר איך לכתוב, אבל **לא מחזיק עותק של
  הטקסטים** — אלה חיים ב-`he.ts` (ממשק), `eventTypes.ts`/`event_terms.py`
  (מונחים), `message_library.py` (WhatsApp) ו-`content/marketing/` (שיווק).
- **סיבה:** לפני ההחלטה לא היה מסמך אחד שמגדיר איך VEYA מדברת — היו עקרונות
  ב-`brand.md` (63 שורות), טקסטים מפוזרים בשלוש שכבות קוד שנכתבו בזמנים שונים,
  ואפס נכס קופי שיווקי. בנוסף, מחקר שוק (2026-07-24) העלה ממצאים שחייבים מקום
  קבוע: הטיעון הכלכלי (מנה ~300 ₪ × 400–650 מוזמנים), לוח הזמנים הישראלי
  המקובל לאישורי הגעה, ו**חמישה רגיסטרים נפרדים של נוסח הזמנה** (קלאסי/
  הומוריסטי/דתי/חב"ד/חרדי) — כולל העובדה שבשלושת האחרונים **ההורים הם
  המזמינים, לא הזוג**, מה ששובר תבניות שמניחות "בני הזוג מזמינים".
- **השפעה:** `brand.md` ו-`veya-master/SKILL.md` §8 מפנים לסקיל החדש.
  נגזרות שלוש עבודות המשך: ביקורת 141 תבניות ה-WhatsApp מול ממצאי המחקר;
  הרחבת `he.ts` מ-3 מקטעים לכיסוי מלא; ובניית `content/marketing/`.
  זוהו שני פערים בספריית ההודעות — אין קטגוריית "עדכון אירוע" (שינוי
  שעה/מיקום), ואין רגיסטר דתי אמיתי (רק `traditional` "מסורתי חם").
- **חלופות שנשקלו:** (א) להוסיף 6-8 קבצים ל-`veya-master/references/` — נדחה,
  הסקיל כבר בן 23 קבצים ומאבד מיקוד; (ב) תיקיית `content/` בלבד בלי סקיל —
  נדחה, לא נטענת אוטומטית כשכותבים טקסט; (ג) לכתוב ספריית הודעות חדשה מאפס —
  נדחה לטובת ביקורת ושדרוג של 141 התבניות הקיימות, כדי לא ליצור שתי ספריות
  שמתפצלות זו מזו.

### 2026-07-22 — סבב Audit שלישי: מנוע הושבה בפרונט, ספריות family/other, שאריות guestsLabel
- **החלטה:** סבב שלישי ואחרון (לפי הוראת המייסד "לסגור אחת ולתמיד") — הפעם
  התמקד בפערים ש-2 הסבבים הקודמים לא כיסו כי הם דרשו לעקוב אחרי שרשרת
  שימוש, לא רק חיפוש טקסט:
  1. **`seatingAdvisor.ts` ("מילוי חכם" בעורך המפה) הפך מודע ל-`event_type`:**
     זה היה מנוע ניקוד **עצמאי ומקביל** לבקאנד (`seating.py`) שכבר תוקן
     בסבב הקודם — כלומר גם אחרי שהבקאנד ידע להעדיף "קבוצה" על "צד" באירוע
     עסקי, ה"מילוי החכם" בפרונט (מה שהמשתמש בפועל לוחץ בעורך המפה) עדיין
     התעלם לגמרי מסוג האירוע. נוסף `SEATING_WEIGHTS_BY_EVENT_TYPE` מקביל,
     עם אותו עיקרון: ברירת מחדל = חתונה לכל הסוגים, רק `business` סוטה.
  2. **ספריות הודעות ייעודיות ל"אירוע משפחתי" ו"אחר":** שני הסבבים הקודמים
     החליטו במפורש להשאיר את שני הסוגים האלה על `GENERIC_LIBRARY` משותפת
     ("אין להם זהות טונאלית ברורה שמצדיקה השקעה") — המייסד ביקש עכשיו
     במפורש לבנות להם ספרייה אמיתית בכל זאת. נכתבו `FAMILY_LIBRARY`
     (חמה/משפחתית) ו-`OTHER_LIBRARY` (ניטרלית-חמה, גמישה לכל אירוע כללי),
     8 תבניות כל אחת, באותו תבנית טוקנים כמו שאר הספריות הייעודיות.
     `GENERIC_LIBRARY` נשארה רק כ-fallback ל-`event_type` לא מוכר בכלל.
  3. **שאריות "מוזמנים" קשיח שדלפו מתחת לרדאר:** ה-Audit הקודם חיווט את
     `guestsLabel` לניווט הראשי (`App.tsx`) וחשב שזה סוגר את הנושא — אבל
     סריקה ישירה (לא דרך subagent הפעם) מצאה עוד ~10 מקומות שלא עברו דרך
     אותה תווית: לשונית "מוזמנים" בעורך מפת האולם עצמו (`HallPage.tsx`),
     טקסט העזרה שמסביר את הלשוניות (שהתייחס בשם לתגית הישנה), שלב "מוזמנים"
     באשף השליחה הראשונה (`RsvpPage.tsx`), וכמה empty-states/הודעות תוצאה
     ("כל המוזמנים כבר משובצים", "הושבת X מוזמנים"). **תובנה מרכזית:** ניווט
     ראשי דינמי לא מבטיח שכל מסך-משנה דינמי — צריך לבדוק כל מסך בנפרד, לא
     להסתמך על זה שה"שער הראשי" תוקן.
  4. **מבחן חובה חדש ננעל בסקיל** (ראה "החלטות נעולות" למעלה): "האם זה
     מתאים לכל סוגי האירועים?" כשאלה מוקדמת חובה לכל פיתוח — לא רק עיקרון
     כללי — בדיוק בגלל הפער בסעיף 3 (ניווט ראשי דינמי הרגיש כמו "סגור" בלי
     שהיה).
- **סיבה:** המייסד ביקש סבב Audit נוסף עם דגש מפורש על "לא לבנות מוצרים
  נפרדים... אותה מערכת, אותה חוויה" ובקש לוודא שגם המנועים (הושבה) וגם
  התוכן (ספריות) מרגישים שנבנו לכל סוג, לא רק שהטקסט הגלוי תוקן.
- **השפעה:** `frontend/src/seatingAdvisor.ts`, `backend/app/message_library.py`
  (FAMILY_LIBRARY/OTHER_LIBRARY חדשות + `_LIBRARY_BY_TYPE`), `HallPage.tsx`,
  `RsvpPage.tsx`. עדכון `SKILL.md` §6 עם המבחן המפורש.
- **חלופות שנשקלו:** לתקן רק את מה שגוגל/משתמש קצה רואה ישירות בלי לעקוב
  אחרי מנועי ניקוד פנימיים (נדחה — המייסד ביקש מפורשות "מילוי חכם" ו"ניקוד
  קבוצות" כסעיף נפרד ומודגש בבקשה); להשאיר family/other על GENERIC (נדחה —
  הוראה מפורשת חדשה מהמייסד עוקפת את ההחלטה הקודמת "לפי ביקוש").

### 2026-07-22 — סגירת Event-first: אדמין, מנוע הושבה, שאריות שיווק/קוד אחרונות
- **החלטה:** Audit נוסף מקצה-לקצה (קוד, אדמין, מנוע הושבה, שיווק, סקיל) לפי
  הוראת המייסד "לסגור את הנושא הזה אחת ולתמיד" — לא רק תיקוני טקסט, גם
  פאנל האדמין ומנוע ההושבה הפכו מודעי-`event_type` בפועל:
  1. **פאנל האדמין:** `AdminEventRow`/`AdminDashboardEvent` (`schemas.py`)
     קיבלו שדה `event_type` + שדה `hosts`/`couple` בנוי דרך
     `event_terms.hosts_names()` (לא עוד `groom_name`/`bride_name` גולמי).
     לוח הבקרה קיבל פילוח **"אירועים לפי סוג"** (`events_by_type`, `GROUP BY
     event_type`). מסך "כל האירועים" קיבל **סינון** לפי סוג (dropdown) בנוסף
     לחיפוש (`AdminApp.tsx`). "זוג" הוחלף ב"בעל/ת אירוע" בתוויות אדמין
     כלליות (`ACCOUNT_TYPE_LABELS`, placeholder-ים, הודעות הסבר).
  2. **מנוע ההושבה מודע-סוג (עדין, לא ארכיטקטוני):** `seating.py` קיבל
     `SEATING_WEIGHTS_BY_EVENT_TYPE` — `event_type` כבר היה מגיע לפונקציות
     הציבוריות (`generate_seating`/`recommend_seats`) בלי לשמש בפועל לניקוד.
     כרגע רק `business` סוטה מברירת המחדל (מדגיש "אותה קבוצה" על פני "אותו
     צד" — לעובד/לקוח/מחלקה יש משמעות רבה יותר מ"צד" באירוע עסקי). **כל שאר
     הסוגים = בדיוק כמו חתונה**, כדי לא לפגוע בחוויה הקיימת. `TOGETHER_BONUS`
     (בקשת "לשבת עם" מפורשת) נשאר קבוע לכל הסוגים במתכוון — בקשה מפורשת של
     המארגן/ת לא אמורה להישקל אחרת בגלל סוג האירוע.
  3. **שאריות שיווק שהאודיט הקודם פספס:** `frontend/public/site.webmanifest`
     (שם/תיאור ה-PWA היו 100% "ניהול חתונות חכם" — משפיע על מסך הבית בהתקנה
     לטלפון) ו-`frontend/public/blog/index.html` (title/meta/OG/Twitter +
     פסקת הפתיחה הציגו "ניהול חתונה" לפני "כל אירוע") תוקנו לניסוח Event-first
     תואם ל-`index.html`.
  4. **שאריות קוד:** `AuthPage.tsx` (מסך התחברות/הרשמה — H1 היה עדיין "החתונה
     שלכם, מאורגנת אחת ולתמיד", וקופירייט "עד היום הגדול" — שניהם ניסוח
     חתונה-ספציפי במסך הראשון שכל משתמש חדש רואה, לפני שיש לו אפילו
     `event_type`); `MessageBuilder.tsx` (טוקני נפילה `{{bride_name}}`/
     `{{groom_name}}` נפלו למילים "הכלה"/"החתן" קשיחות במקום ל-
     `terms.hostAField`/`hostBField`); `messaging.py`/`automation.py`
     (תיאורי placeholder כמו "שם הכלה" הוחלפו לניסוח דו-משמעי "שם בעל/ת
     האירוע... (בחתונה — הכלה)"); `confirm.py` ("בקשו מבעלי השמחה קישור חדש"
     → "מארגני האירוע", כי הודעת שגיאה ברמת הטוקן מוצגת לפני שיש הקשר לאירוע
     ספציפי); `admin.py` תיעוד פנימי ("כל זוג חדש" → "כל אירוע חדש").
  5. **ניקוי קוד מת:** `AdminPage.tsx` הכיל קומפוננטת `AdminPage()` שלמה
     (כפילות טבלת משתמשים/אירועים, כולל `groom_name`/`bride_name` גולמי) —
     לא מיובאת משום מקום (`AdminApp.tsx` הוא הפאנל החי היחיד). נמחקה במלואה,
     כולל ה-imports היתומים שנשארו רק בשבילה.
  6. **באג אמיתי שנתפס ע"י בדיקה חיה (לא רק סקירת קוד):** הוספת `label`
     ל-`EventTerms` ב-backend בלי לעדכן את כל 8 הרשומות ב-`EVENT_TERMS`
     הפילה את `/admin/dashboard` (500 → "Failed to fetch" בדפדפן) — נתפס
     מיד כי כל שינוי הורץ בפועל מול השרת המקומי לפני commit, לא רק `tsc`/
     `import` נקי. תוקן והורץ מחדש עד לאימות ויזואלי אמיתי.
- **סיבה:** הוראת המייסד: "רק כשאתה בטוח שאין יותר שרידים... בצע Commit".
  זה חייב עבודה שמעבר לטקסט גלוי — אדמין ומנוע ההושבה הם משטחים שמפתח/ת
  חדש/ה יפגוש ולא "features" שמשתמש קצה רואה ישירות, אבל הם חלק מהחוויה.
- **השפעה:** ראו רשימת קבצים בדוח הסיום למייסד. עדכון מקביל ל-
  `product-state.md` (פאנל אדמין + מנוע הושבה Event-aware), `architecture.md`
  (סעיף חדש "איך מוסיפים סוג אירוע חדש" + "מה אסור לעשות"), `open-questions.md`.
- **חלופות שנשקלו:** לתת למנוע ההושבה משקלים שונים לכל אחד מ-8 הסוגים
  (נדחה — בלי נתוני שימוש אמיתיים זה ניחוש; רק `business` קיבל שינוי כי יש
  לו נימוק מוצרי ברור וחד — "צד" כמעט לא רלוונטי לאירוע עסקי); לבנות endpoint
  נפרד לסטטיסטיקות אדמין לפי סוג (נדחה — הרחבת `AdminDashboard` הקיים
  פשוטה יותר ותואמת לעיקרון "לא לבנות מערכות נפרדות").

### 2026-07-22 — השלמת Event-first: קבוצות דינמיות, ספריות הודעות ייעודיות, דף נחיתה כמערכת אירועים
- **החלטה:** המשך ישיר לפיבוט Event-first architecture — סגירת הפערים
  שנותרו "מרגישים כמו מערכת חתונות" לפי הוראת המייסד, בלי לבנות מערכות
  נפרדות (אותו `event_type`, אותו לקסיקון, אותה ארכיטקטורה):
  1. **קבוצות מוזמנים דינמיות** — נוסף `groupOptions` (frontend)/
     `group_options` (backend) ללקסיקון: חתונה/חינה נשארות עם הרשימה
     הקיימת (משפחה קרובה/רחוקה, חברים, עבודה, צבא, לימודים, חברי ילדות,
     שכנים — **ללא שינוי**, לא לפגוע בחתונה); בר/בת מצווה מקבלים משפחת
     אב/משפחת אם/חברים/כיתה/צוות-חוגים; ברית/משפחתי/אחר מקבלים משפחה/
     חברים; עסקי מקבל עובדים/לקוחות/ספקים/הנהלה/שותפים. `group_type`
     ב-DB כבר היה טקסט חופשי ללא אכיפת enum — לא נדרשה מיגרציה, רק
     הרחבת הרשימות המוצעות בטפסים (`AddGuestForm.tsx`, `PasteImportDialog.tsx`)
     והמיפוי בייבוא Excel/הדבקה (`importer.py: GROUP_VALUE_MAP`, עם תיקון
     שגיאת דקדוק דומה לזו שכבר נפתרה ב-`SIDE_VALUE_MAP`: "אם"/"אמא" הן
     אותיות שונות ב-Unicode בגלל אות סופית — צריך את שתיהן כמפתחות נפרדים).
     `routers/stats.py` פילוח `by_group` הפסיק להיות מאותחל עם 9 הקטגוריות
     החתונתיות הקבועות — עכשיו נבנה מ-`groupOptions` של הסוג הפעיל.
  2. **ספריות הודעות ייעודיות (לא רק גנרי):** נכתבו מאפס — לא רק טוקנים
     מוחלפים בתוך תבנית משותפת — `HENNA_LIBRARY` (10, חגיגי-צבעוני),
     `MITZVAH_LIBRARY` (10, משפחתי-גאה, משותפת לבר+בת מצווה), `BRIT_LIBRARY`
     (8, משפחתי-אינטימי), `BUSINESS_LIBRARY` (10, מקצועי-תמציתי, בלי
     אימוג'י חגיגי). אירוע משפחתי/אחר נשארו על `GENERIC_LIBRARY` (12) —
     אין להם זהות טונאלית מובהקת עדיין. חתונה נשארה על `LIBRARY` (75),
     ללא שינוי.
  3. **דף נחיתה + SEO — H1 מ"חתונה" ל"אירוע":** לפי בקשת המייסד המפורשת
     ("VEYA היא מערכת חכמה לניהול אירועים... לא להגביל את המותג לחתונות")
     — ה-H1 עבר מ"כל החתונה שלכם" ל"**כל האירוע שלכם**". חתונה עדיין
     מוזכרת **ראשונה** בכל רשימת סוגים (hero-trust chip, meta description,
     FAQ, Schema) — לא נמחקה, רק אינה עוד הכותרת הבלעדית. נוספה כרטיסיית
     פיצ'ר "ספריית הזמנות לכל סוג אירוע" בעמוד הפיצ'רים, ו-`SEO_STRATEGY.md`
     עודכן (§0, §2.4) לשקף שהמסר המאוחד כבר חי בדף הבית, לא רק מתוכנן.
- **באגים אמיתיים שנמצאו ותוקנו תוך כדי (לא רק ניסוח):**
  - **דקדוק ב-`BRIT_LIBRARY`:** "נחגוג **את** ברית של X" שגוי (מילת היחס
    "את" דורשת יידוע — "ברית של" אין לו ה"א מובנית כמו "בר המצווה **ה**ל"
    שבו ה-ה כבר בתוך "המצווה"). תוקן ל"מזמינים אתכם **ל**ברית של X" — "ל"
    לא דורש יידוע. אומת ברינדור בפועל לפני ואחרי.
  - **חסימת `deps.py` לפיתוח מקומי (לא קשור ל-Event-first עצמו, התגלה
    תוך כדי בדיקה חיה):** עבודה מקבילה על RLS הוסיפה ל-`EventAccess`
    קריאת `current_setting()` (Postgres-בלבד) שמפילה כל בקשה עם
    `X-Event-Id` מול SQLite מקומי — כולל שורת דיבאג `print`. **לא תוקן
    כאן** (שייך לעבודה המקבילה, לא נגענו בקבצים שהיא באמצע עריכתם) —
    תועד כחוב טכני ושאלה פתוחה. בגללו לא ניתן היה להשלים בדיקת הוספת-
    מוזמן חיה מקצה-לקצה לאירוע העסקי; הווידוא נעשה בשילוב תצוגת ה-UI
    (בורר הקבוצות מציג נכון) + בדיקות Python ישירות על שכבת הרינדור.
- **סיבה:** המשך ישיר להוראת המייסד "לסגור את כל המקומות שבהם עדיין מרגישים
  שזו מערכת חתונות בלבד" — לא רק תשתית (Task קודם), אלא תוכן וטקסונומיה
  אמיתיים שמורגשים במסכים בפועל.
- **השפעה:** `frontend/src/strings/eventTypes.ts`, `backend/app/event_terms.py`,
  `frontend/src/types.ts` (GROUP_LABELS הפך ל-superset), `AddGuestForm.tsx`,
  `PasteImportDialog.tsx`, `backend/app/importer.py`, `backend/app/message_library.py`
  (~450 שורות תבניות חדשות), `frontend/index.html`, `SEO_STRATEGY.md`.
  **לא הוגש:** `routers/guests.py`/`routers/stats.py` מכילים גם שינויים
  מקבילים (RLS) — הקוד שלנו שם נכתב ואומת מקומית אבל לא הוגש ל-git כי
  הקבצים מעורבבים עם עבודה לא-קשורה שעדיין בעריכה.
- **חלופות שנשקלו:** לצמצם קבוצות עסקי לרשימה "כללית" יחידה בלי פירוט
  (נדחה — המייסד ביקש פירוט מפורש: עובדים/לקוחות/ספקים/הנהלה/שותפים);
  להשאיר H1 חתונה-first ורק את גוף הדף להרחיב (נדחה — המייסד ביקש במפורש
  לא להגביל את ה-H1/מסר-על לחתונה); לתקן את `deps.py` בעצמנו כדי לאפשר
  בדיקה חיה מלאה (נדחה — קובץ באמצע עריכה של עבודה מקבילה, סיכון לדרוס
  שינויים לא-קשורים).

### 2026-07-22 — בדיקה חיה (5 סוגי אירוע בדפדפן) חשפה 2 באגים אמיתיים שהאודיט הקודם פספס
- **החלטה:** אחרי הפיבוט ל-Event-first architecture, נבדקה המערכת **כמשתמש
  אמיתי בדפדפן** (לא רק בקוד/ב-unit test) — נוצרו 5 אירועים אמיתיים דרך ה-UI:
  חתונה, בת מצווה (מקבילה מבנית לבר מצווה — שני הסוגים אומתו זהים ב-Python
  לפני כן), חינה, ברית, אירוע עסקי. הבדיקה החיה חשפה 2 באגים שסקירת קוד
  סטטית לא הייתה תופסת:
  1. **`OnboardingWizard.tsx` לא סנכרן את סוג האירוע הפעיל ל-store:** שלב 2
     של האשף (הוספת מוזמנים ראשונים) קרא ל-`activeEventTerms()` שנשען על
     `getActiveEventType()` (localStorage), אבל `submitDetails()` קרא רק
     ל-`setEventId()` ולא ל-`setActiveEventType()` — כך שאם היה סוג אירוע
     אחר פעיל קודם באותו דפדפן (למשל session קודם), שלב 2 הציג תוויות צד
     **שגויות** (מהסוג הקודם, לא מהסוג שנבחר). App.tsx הראשי כבר עשה את
     הסנכרון הזה נכון בכל רינדור — הפער היה ספציפית בחלון הזמן שבין יצירת
     האירוע לכניסה למעטפת האפליקציה הראשית. **תוקן:** קריאה ל-
     `setActiveEventType(ev.event_type)` מיד אחרי `setEventId()`.
  2. **5 תבניות ה-VeyaTemplate הגלובליות שנזרעות ב-`seed_veya_defaults()`
     (`main.py`) הן ברירת המחדל ל*כל* אירוע חדש מכל סוג, ולא היו Event-
     ready:** נוסח ההזמנה הראשוני אמר "ונשמח מאוד לראות אתכם **ולחגוג
     יחד**!" עם אימוג'ים חגיגיים (✨❤️🎉🥂) — ראינו את זה בפועל באירוע עסקי
     ("אנחנו חברת נובה טק, ונשמח מאוד לראות אתכם ולחגוג יחד!"). הטוקנים
     עצמם היו דינמיים נכון (השם הציג "חברת נובה טק" נכון) — רק הניסוח
     סביבם היה נעול לטון חגיגי-חתונתי. **תוקן:** הוסר "ולחגוג יחד" ואימוג'ים
     חגיגיים (✨❤️🎉🥂→ הוסרו/הוחלפו ב-🙏 במקומות המתאימים) משכל 5 התבניות,
     בלי לפגוע בחום/במקצועיות עבור חתונה.
  - **מלכודת אימות שגילינו:** תבנית שכבר "התממשה" לאירוע קיים (הועתקה
    מה-VeyaTemplate הגלובלי ל-Message של האירוע הספציפי) **לא מתעדכנת
    למפרע** מ-reseed של הטבלה הגלובלית — רק אירועים חדשים שנוצרים אחרי
    ה-reseed מקבלים את הנוסח המתוקן. זו בדיוק הבעיה שכבר תועדה כשאלה פתוחה
    #30 (reseed בפרודקשן) — הבדיקה החיה **אישרה שהיא אמיתית ולא תיאורטית**.
- **סיבה:** "לבדוק כמו משתמש אמיתי, לא רק דרך הקוד" (הוראת המייסד) — קוד
  שנראה תקין בקריאה (`activeEventTerms()` נראה "כבר דינמי") יכול עדיין לפגוע
  במשתמש בגלל תזמון/state גלובלי שלא נבדק בזרימה מלאה.
- **השפעה:** `OnboardingWizard.tsx`, `main.py` (5 תבניות הזרע). מאומת: build
  מלא (`tsc`+`vite build`) ו-import מלא של ה-FastAPI app עברו נקי; 5 האירועים
  נבדקו ידנית מקצה לקצה כולל שלב 2 של האשף אחרי התיקון.
- **חלופות שנשקלו:** להסתפק בסקירת קוד סטטית בלבד (נדחה — בדיוק ככה שני
  הבאגים האלה היו נשארים; רק ריצה אמיתית תפסה את פער ה-timing ב-store ואת
  הטון בתבנית שמומשה בפועל).

### 2026-07-22 — Audit מלא: סגירת פערי Event-ready שנשארו אחרי הפיבוט
- **החלטה:** ביקורת קוד ממוקדת (לא רק תיעוד סקיל) מצאה שהארכיטקטורה הדינמית
  (Lexicon) נבנתה נכון, אבל כמה מקומות **עקפו** אותה עם טקסט "חתונה" קשיח
  ישירות בקומפוננטה/פונקציה. כל הממצאים תוקנו; "הוספת סוג אירוע = רשומה אחת
  בלקסיקון, בלי לגעת במסכים" עכשיו **נכון בפועל**, לא רק בתיאוריה.
- **מה נמצא ותוקן (קטגוריות):**
  - **קהל יעד אוטומציה:** `TARGET_GROUP_LABELS.side_groom/side_bride` היה קבוע
    ל"צד החתן/צד הכלה" — עכשיו `targetGroupLabel()` דינמי (`types.ts`).
  - **מנוע ההושבה בבקאנד:** פאנל "למה שובץ כאן" (`seating.py: _reasons_for`,
    `recommend_seats`) קידד "(חתן/כלה)" קשיח. נוסף `side_axis_label()` ל-
    `event_terms.py` (עם שדות `side_groom`/`side_bride` חדשים ב-`EventTerms`),
    ו-`event_type` הוזרם מ-`routers/seating.py` עד לפונקציות הפנימיות.
  - **עוזר הושבה חכם (פרונט):** אזהרת ערבוב-צדדים ב-`seatingAdvisor.ts` קידדה
    "מצד החתן/מצד הכלה" קשיח — עכשיו דרך `sidePhrase()` חדש ב-`eventTypes.ts`.
  - **ייבוא Excel:** `importer.py: SIDE_VALUE_MAP` זיהה רק ערכי "חתן/כלה" בעמודת
    "צד" — מוזמנים עם ערכים כמו "אב/אם"/"א׳/ב׳" (בר מצווה, אירוע משפחתי) היו
    נופלים שקט ל"משותף". נוספו הערכים החסרים.
  - **מסך התחברות/הרשמה (`AuthPage.tsx`):** הפאנל השיווקי (שמוצג *לפני* בחירת
    סוג אירוע) והכפתור "הרשמה כזוג" הניחו זוג מתחתן תמיד — רוככ ל"הרשמה
    ל-VEYA" ולתגית שמזכירה גם אירועים אחרים.
  - **פאנל אדמין:** כותרת עמודה "חתן / כלה" (מציגה אירועים מכל הסוגים
    מעורבבים), וכפתור התחזות "כניסה כזוג" — הפכו לניסוח כללי ("בעלי האירוע",
    "כניסה כבעלים").
  - **קוד מת שהיה עלול להטעות פיתוח עתידי:** `SIDE_LABELS` (קבוע לא-בשימוש
    ב-`types.ts`, כפילות שגויה של `sideLabel()` הדינמי), וכמה מפתחות לא-
    בשימוש ב-`strings/he.ts` (`groomPlaceholder`, `bridePlaceholder`,
    `imageBubbleLabel`, `inviteImgAlt`, `coupleFallback`) — הוסרו.
  - **הבלוג הציבורי:** פוטר ודף הבלוג הראשי (Hub) הורחבו ל"חתונות ואירועים",
    בהתאמה למה שכבר נעשה בדף הבית. **המדריך הבודד נשאר ממוקד-חתונה בכוונה**
    (עקרון "כאב אחד לעמוד" מ-`seo.md`/`SEO_STRATEGY.md`) — לא תוקן, ראה למטה.
- **גבול ארכיטקטוני שתועד (לא באג, אבל שווה לדעת):** `Side` בשני הצדדים הוא
  `Literal["groom", "bride", "shared"]` — מפתחות פנימיים קבועים, עם תווית
  תצוגה דינמית לפי סוג (`sideLabels`/`side_groom`/`side_bride`). זה עובד
  מצוין לכל 8 סוגי האירוע היום (כולם צריכים לכל היותר 2 "צדדים" קונספטואליים
  + "משותף"). **אם אי-פעם יתווסף סוג אירוע שדורש 3+ "צדדים" נבדלים** (לא
  קיים היום), זה ידרוש שינוי סכימה אמיתי (DB + Literal), לא רק רשומת לקסיקון.
  ראה `open-questions.md` #32.
- **סיבה:** "לא להמציא תשובה, לבדוק בקוד" — הביקורת האמיתית היא ב-code, לא
  רק בתיעוד. הפיבוט הקודם בנה את המנגנון נכון; חלק מהמסכים/פונקציות פשוט
  לא "התחברו" אליו עדיין. באג משתמש (טקסט לא נכון) שונה מפער תיעוד.
- **השפעה:** 17 קבצים (בקאנד + פרונט + בלוג). `tsc --noEmit`, `vite build`
  ו-import מלא של ה-FastAPI app עברו נקי אחרי כל תיקון. נבדק פוזיטיבית
  שחתונה מרונדרת **זהה בדיוק** לפני/אחרי בכל המקומות שתוקנו (0 regression).
- **חלופות שנשקלו:** לשנות את מבנה ה-DB של `Side` ל-Enum כללי יותר (למשל
  `side_a`/`side_b`) כדי "לנקות" את השם (נדחה — לא נדרש כרגע, כל הסוגים
  הקיימים ממופים נכון דרך התווית הדינמית; שינוי כזה הוא מיגרציה לא-הכרחית
  היום — ראה כלל "לא לשנות שמות פנימיים בלי ערך עתידי ברור" ב-`SKILL.md`).

### 2026-07-21 — סנכרון מלא של הסקיל לפיבוט Wedding-first, Event-ready
- **החלטה:** לאחר שהפיבוט ל-Event-ready יושם בקוד (Lexicon, `event_type`,
  `entries_for`), הושלם סנכרון הסקיל בכל הקבצים הרלוונטיים: `brand.md` קיבל
  סעיף כללי שפה דינמית (Copywriting Rules — מתי חתן/כלה, מתי בעלי-אירוע/
  מארגני-אירוע/משפחה/משתתפים/אורחים); `product-principles.md` קיבל מבחן שישי
  "Event-ready" חובה לכל פיצ'ר; `design-system.md` ו-`architecture.md` תיעדו
  את כלל ה-Lexicon (`event_type` כמקור התאמה יחיד, אין טקסט קשיח במסכים);
  `product-state.md` ו-`roadmap.md` תיעדו את הפער האמיתי (ספריית הודעות
  גנרית משותפת ל-7 סוגים, לא ייעודית); `open-questions.md` קיבל שאלת עומק
  לספרייה.
- **סיבה:** הוראת המייסד — הסקיל הוא מקור האמת של הפרויקט, וכל שינוי משמעותי
  (זה כולל את הפיבוט עצמו) חייב להתעדכן בו באותו תהליך עבודה, לא בנפרד.
- **השפעה:** כל 8+ הקבצים הרלוונטיים עקביים עם המציאות בקוד; אין עוד "חתן/
  כלה" מקובע כברירת מחדל בהנחיות הסקיל למקומות תלויי-אירוע.
- **חלופות שנשקלו:** קובץ Copywriting נפרד (נדחה — `brand.md` כבר מכיל את קול
  המותג והמילון, הרחבתו שומרת על מקור אמת אחד ולא מפזרת).

### 2026-07-21 — אזור פתיחה כהה בדף הנחיתה כדי להציג נכון את הלוגו הרשמי
- **החלטה:** ה-hero (וגם ה-band וה-footer) בדף הנחיתה הם **כהים וחמים** (פחם עמוק
  `#17130d` עם זוהר זהב עדין), בעוד גוף הדף נשאר שנהב חם. הלוגו הרשמי מוצג גדול
  ובולט ב-hero (`logo.png` המלא עם התגית) ובהדר/פוטר (`logo.svg` האופקי).
- **סיבה:** הלוגו הרשמי בנוי בגוונים בהירים (מילת "VEYA" בצבע שמנת `#E6E0D4` +
  זהב) ותוכנן לרקע כהה — על השנהב הבהיר הוא כמעט בלתי נראה (יחס ניגודיות ~1.1).
  זו הייתה הסיבה ש"הלוגו לא הופיע נכון". רקע כהה פותר את הנראות, ובו-זמנית משדר
  פרימיום מיידי (אסתטיקת הזמנת חתונה עם הטבעת זהב) — רושם ראשוני חזק.
- **השפעה:** `frontend/index.html` (דף נחיתה MVP סטטי). לא נוצר לוגו חדש —
  שימוש בנכסים הקיימים בלבד. שנהב נשאר צבע-העל של המותג בגוף הדף.
- **חלופות שנשקלו:** לצבוע מחדש את הלוגו לגרסה כהה (נדחה — המייסד ביקש לא לשנות
  את הלוגו); להציג טקסט "VEYA" בלבד בלי הלוגו (נדחה — לא מכובד/פרימיום);
  hero בהיר עם הלוגו (נדחה — הלוגו בלתי נראה על שנהב).

### 2026-07-21 — ניהול רזרבה חכם כחלק אינטגרלי ממערכת ההושבה
- **החלטה:** מוסיפים ניהול רזרבה בתוך מסך האולם (לא מערכת נפרדת) בשני סוגים:
  (1) **רזרבה מפוזרת** — יעד מקומות פנויים (`event.reserve_seats`) שמפוזר אחיד בין
  השולחנות הפעילים בשיבוץ האוטומטי; (2) **שולחן רזרבה** (`is_reserve` בכל שולחן) —
  מוצא מהמנוע האוטומטי ושמור לשיבוץ ידני. נוסף **מצב יום האירוע** (פאנל צד במסך
  האולם) עם שיבוץ מהיר בקליק + **המלצה דטרמיניסטית** (`recommend_seats`) ואזהרות
  רכות שאינן חוסמות. אין שיבוץ אוטומטי בלי אישור המשתמש.
- **סיבה:** חתונה משתנה ברגע האחרון (ביטולים/תוספות). בלי רזרבה מובנית, כל שינוי
  מפיל את כל השיבוץ. זה מוריד לחץ, חוסך זמן, ושומר על החוויה ביום עצמו.
- **השפעה:** `models.py` (עמודת `reserve_seats`), `schemas.py`, `routers/seating.py`
  (3 endpoints: recommend-seat/assign/reserve), `routers/hall.py`, `seating.py`
  (`recommend_seats`), פרונט (HallPage מצב יום האירוע + בורר רזרבה, DashboardPage
  כרטיס רזרבה). `seating-engine.md`, `product-state.md`.
- **חלופות שנשקלו:** רזרבה ברמת שולחן בודד בלבד (נדחתה — פחות גמיש); שיבוץ
  אוטומטי של אורח הרגע-האחרון בלי אישור (נדחה — סותר את עקרון "לא לנחש", והמשתמש
  צריך שליטה ביום עצמו); מסך רזרבה נפרד (נדחה — סותר "פשטות", עדיף אינטגרלי).
- **נאמנות לגבול הנעול:** ההמלצה משתמשת **באותם משקלים דטרמיניסטיים** של המנוע —
  ה-LLM לא מעורב בבחירת השולחן. הדטרמיניזם והחוקים הקשיחים נשארים.

### 2026-07-21 — חידוד מיצוב: הליבה היא ניהול חכם ופשוט, ה-AI מנוע מעצים
- **החלטה:** הליבה של VEYA היא **ניהול חכם ופשוט של החתונה**. ה-AI הוא **מנוע**
  שמעצים את החוויה, מפחית עבודה ידנית ומציע אוטומציה — **לא המטרה בפני עצמה**.
  מחליף את הניסוח הקודם "ה-AI הוא הליבה, לא תוסף" ב-`vision.md` וב-`ai-guidelines.md`.
- **סיבה:** אמירת מיצוב מפורשת של המייסד. AI שמוצג כמטרה מסכן פשטות וחוויה;
  כשהמטרה היא ניהול פשוט, כל שילוב AI נבחן לפי כמה הוא מפשט — לא לפי כמה הוא "חכם".
- **השפעה:** `vision.md`, `ai-guidelines.md`; כל החלטת פיצ'ר AI עתידית נמדדת
  מול הליבה (פשטות/הורדת עבודה), לא מול "כמה AI". **לא** משנה את הגבול הנעול
  שה-LLM אינו מחשב הושבה — הדטרמיניזם נשאר.
- **חלופות שנשקלו:** להשאיר "AI הוא הליבה" (נדחה — מבלבל מטרה עם אמצעי, ועלול
  להצדיק הוספת AI שמסבך במקום לפשט).

### 2026-07-21 — שיבוץ אוטומטי מודע-הערות ומודע-מיקום
- **החלטה:** הסידור בקליק אחד מכבד עכשיו הערות מוזמן + הערות קבוצה כהעדפות
  אזור (רחבת ריקודים/בר/כניסה/ריחוק מרעש/נגישות/מבוגרים/הריון) עם ניקוד לפי
  מיקומי השולחנות האמיתיים במפה, מציג פאנל "למה שובץ כאן", והתאמת "לא לשבת
  יחד"/"לשבת עם" היא **מכלילה** (שם פרטי → כל בעלי השם; "משפחת X" → כל המשפחה).
- **סיבה:** שהמשתמש ירגיש מיד שהמערכת "הבינה" את ההערות; מכליל כי כשכותבים
  "לא לשבת ליד דני" מתכוונים לכל הדנים, ואיסור על אדם אחד בלבד לא ענה לצורך.
- **השפעה:** `seating.py`, `constraints.py`, `routers/seating.py`, `schemas.py`,
  פרונט (`HallPage.tsx`, `App.css`, `types.ts`). תואם לעיקרון הנעול —
  הפרשנות מ-keyword-matching דטרמיניסטי, בלי LLM, החישוב נשאר דטרמיניסטי.
- **חלופות שנשקלו:** נגישות כחוק קשיח (נדחה — אין דגל נגישות פר-שולחן ב-DB,
  היה עלול להשאיר מוזמנים ללא מקום; נבחר "העדפה חזקה מאוד"); התאמה חד-משמעית
  עם הבהרה (נדחה לשיבוץ האוטומטי — רוצים להכליל את כולם, לא לשאול).

### 2026-07-21 — הרחבת הסקיל לאחר Audit מקצועי
- **החלטה:** לאחר Audit רב-עדשתי הורחב הסקיל: נוספו קבצי
  `compliance-security-privacy.md`, `business-model-finance.md`,
  `customer-success-support.md`, `risk-register.md`, `data-strategy.md`;
  `growth-seo.md` פוצל ל-`growth.md` + `seo.md`; `ai-guidelines.md` קיבל שכבת
  אסטרטגיה; נוסף מבחן "אופק 5 שנים / 5 קהלים" ל-SKILL.md.
- **סיבה:** כיסוי פערים קריטיים (פרטיות/PCI, מודל עסקי, סיכונים) שחסרו לקבלת
  החלטות נכונות לטווח ארוך, בלי להמציא מידע — הלא-ודאי סומן כ-Open Question.
- **השפעה:** הסקיל מכסה עכשיו גם משפט/כספים/סיכונים; מפת הידע ב-SKILL.md עודכנה.
- **חלופות שנשקלו:** להשאיר את הסקיל כפי שהיה (נדחה — פערים מהותיים);
  ליצור קובץ לכל נושא Medium (נדחה — מורכבות מיותרת, הועדף להרחיב קיימים).

### 2026-07-21 — יצירת VEYA MASTER SKILL
- **החלטה:** בניית סקיל אסטרטגי מרכזי ב-`.claude/skills/veya-master/` שגורם
  לפעול כחבר צוות מייסדים (7 כובעים, סדר חשיבה 10 שלבים, 5 שאלות החלטה).
- **סיבה:** לרכז חזון + מותג + מוצר + טכני + צמיחה למקום אחד, לעקביות החלטות.
- **השפעה:** כל משימה עתידית ב-VEYA נשענת על הסקיל; `CLAUDE.md` מצביע אליו.
- **חלופות שנשקלו:** תיעוד קוד בלבד (נדחה — לא נותן "מוח אסטרטגי").
