# VEYA Admin + מרכז שליטה בטלפנים — שלב 1: Audit וארכיטקטורה

> נכתב 2026-09-16. מבוסס על קריאת הקוד בפועל (לא על ה-UI). כל מספר כאן נמדד.
> מסמך עבודה: מתעדכן בסוף כל Phase.

---

## חלק א' — מה קיים היום (Audit)

### א.1 מעטפת האדמין (Frontend)

| רכיב | קובץ | שורות | הערה |
|---|---|---|---|
| מעטפת + Sidebar + רוב המסכים | `frontend/src/components/AdminApp.tsx` | 1,750 | ניווט ב-`useState`, אין URL — אי אפשר לשתף קישור למשתמש/אירוע |
| ברירות מחדל להודעות + יצירת חשבון | `AdminPage.tsx` | 496 | |
| Call Center | `AdminCallCenter.tsx` (+css) | 719 | |
| ניהול טלפנים | `AdminCallers.tsx` (+css) | 431 | **מוטמע בתוך מסך "משתמשים"** (AdminApp.tsx:973), לא מסך משלו |
| קבלת מתנות (Payout) | `AdminPayoutReview.tsx` | 324 | |
| בקשות דחייה | `AdminPostponements.tsx` | 266 | |

Sidebar נוכחי (9 פריטים, שטוח): לוח בקרה · Call Center · משתמשים · אירועים · אולמות ·
קבלת מתנות · נוהל דחייה · הודעות ומסלול · יומן פעולות.

### א.2 Backend — Admin API קיים (38 endpoints מוגנים)

כולם מוגנים בשרת (לא רק ב-UI):
- `routers/admin.py` — 27 endpoints, `get_current_admin`: דשבורד, משתמשים (רשימה/פרטים/עדכון/השבתה/הפעלה/מחיקה בשני מצבים/איפוס סיסמה/**Impersonation**), אירועים (רשימה/מחיקה), יצירת חשבון, טלפנים + הקצאה לאירוע, ברירות מחדל להודעות + ספריית נוסחים, סטטיסטיקת הודעות, יומן, אולמות (רשימה/עריכה/מחיקה/מיזוג).
- `routers/payout_admin.py` — 7 (אישור/דחייה/פתיחה מחדש/סטטוס ספק/תעודה).
- `routers/postpone_admin.py` — 4 (אישור/דחייה).
- `routers/call_center.py` — 4, מוגן ב-`get_current_caller` (אדמין **או** טלפן).

### א.3 הרשאות — מה באמת קיים

- **שני צירים בלבד** (`app/roles.py`): `User.is_admin` (בוליאני) + `User.account_type`
  (`couple` / `planner` / `venue` / `phone_agent`).
- **אין דרגות אדמין.** כל אדמין יכול הכול — כולל מחיקת משתמש ו-Impersonation.
- טלפן נחסם בשכבות: `get_current_admin` דוחה אותו במפורש; `EventAccess` דוחה אותו מכל endpoint של אירוע; RLS ב-Postgres (`rls/07_phone_agent_rls.sql`).
- הרשאות חבר-אירוע (מפיק/אולם) — רשימות ב-`app/permissions.py`, מסונכרנות מול RLS בטסט.
- ✅ אבטחה בסיסית טובה: JWT + `token_version`, RLS על כל הטבלאות הרגישות, 18 קבצי RLS.

### א.4 מה **לא** קיים בכלל (אל תניחו שכן)

| תחום | מצב |
|---|---|
| Feature flags | **אין.** יש מתג סביבה אחד: `VEYA_GIFT_ENABLED` (Render env) |
| הגדרות מערכת ב-DB | **אין.** הכול קבועים בקוד או משתני סביבה |
| Event overrides | **אין** |
| מסלולים / מחירים / תוספים / קופונים / מנויים | **אין שום טבלה.** אין תשלום על המוצר |
| דרגות אדמין (RBAC) | **אין** |
| יומן אדמין נפרד | **אין.** טבלת `audit_logs` אחת מערבבת אבטחה + פעולות אדמין + פיד פעילות של בעל האירוע (שיחות טלפן נכתבות אליה כ-`guest_call_*`) |
| מאגר אולמות מלא | **רזה מאוד:** `name, address, city, dedup_key, usage_count`. נבנה אוטומטית מאירועים. אין תמונות/סטטוס/קיבולת/סוגי אירוע |
| תזמון רקע (cron) | **אין.** שליחות WhatsApp מופעלות ידנית (`/due/send`, `/track/advance`) |
| חיפוש גלובלי | **אין** |

### א.5 הגדרות עסקיות שמפוזרות בקוד (מועמדות לשליטה מהאדמין)

| הגדרה | ערך היום | מיקום |
|---|---|---|
| מבנה המסלול: בקשה + 3 תזכורות + 3 סבבי שיחות | קבוע | `rsvp_timeline.py: CYCLE` |
| אורך חלון מקסימלי | 14 יום | `MAX_WINDOW_DAYS` |
| ברירת מחדל סגירת רשימה לאירוע קרוב | יום לפני | `DEFAULT_NEAR_COMMIT_DAYS` |
| טווח בחירת סגירה | 1–10 ימים | `MAX_COMMIT_DAYS` |
| פער WhatsApp↔שיחות / WhatsApp↔WhatsApp | 2 / 1 ימים | `GAP_WA_CALL`, `GAP_WA_WA` |
| חסימת שישי-שבת | קבוע | `_is_weekend` |
| שעת שליחה ברירת מחדל | 16:00 | `Event.rsvp_send_time` (ברמת אירוע כבר!) |
| עמלת מתנות | 4% שלם, מתווספת על הנותן (🔒 נעול) | `gift.py: GIFT_FEE_PERCENT` |
| WhatsApp mock/live | `mock` | env `WHATSAPP_MODE` |
| מתנות פעיל | כבוי | env `VEYA_GIFT_ENABLED` |
| ספק תשלום / Payout | `mock` | env |

### א.6 כפילויות ובעיות מבנה

1. **משתמשים ↔ טלפנים**: ניהול טלפנים מוחבא בתוך מסך משתמשים.
2. **"הודעות ומסלול"** — מסך נפרד; לפי הבקשה צריך להיבלע ב"כללי המערכת".
3. **יומן פעולות** — מציג את כל `audit_logs` כולל רעש (`confirm_invalid_token`, פיד שיחות).
4. **דשבורד** — 6 מונים כלליים + גרף הרשמות + 8 אירועים אחרונים; לא עונה על "מה צריך לעשות עכשיו".
5. **אין URL לאדמין** — אין deep link ל-Event Control Center / ל-Global Search.

### א.7 בסיס בדיקות (לפני כל שינוי)

- Backend: **637 עוברים**, 4 נכשלים, 4 שגיאות איסוף — **כולם קיימים מראש**, לא קשורים לאדמין:
  - 4 קבצים מייבאים מודול שכבר לא קיים (`app.message_library`).
  - `test_phone_agent_permissions` — `/public/library` ציבורי במכוון; רשימת ההיתרים בטסט לא עודכנה (לא דליפה).
  - `test_audit_log_race`, `test_create_event_identity_regression` — דורשים Postgres אמיתי.
- Frontend: `tsc -b` עובר נקי.

---

## חלק ב' — Audit מרכז הטלפנים (Source of Truth)

### ב.1 איך זה עובד היום — בדיוק

```
Event.event_date + Event.venue_commit_days_before
        │  resolve_commit_days()   (לא נבחר + אירוע < 14 יום → יום לפני)
        ▼
compute_schedule()  ← עוגן: rsvp_track_started_at (יום הפעלת המסלול)
        │  פורס את CYCLE לימים פעילים (בלי שישי/שבת), סבב שיחות אחרון = יום סגירת הרשימה
        ▼
due_call_round(event, now) = סבב השיחות האחרון שתאריכו ≤ היום
        ▼
build_queues(): מוזמנים באירוע עם rsvp_status ∈ {pending, maybe} ועם טלפון
        │  מינוס: ניסיון (no_answer/busy) בסבב הנוכחי · callback עתידי · מספר שגוי שלא תוקן
        ▼
תור השיחות — מחושב חי בכל בקשה. אין טבלת משימות.
```

| שאלה | תשובה בקוד |
|---|---|
| מועד סגירת רשימה | `venue_commit_days_before` (1–10) או ברירת מחדל יום לפני לאירוע קרוב; מוזז אחורה משישי/שבת |
| חישוב סבבים | `rsvp_timeline.compute_schedule` — טהור, דטרמיניסטי, נבדק בטסטים |
| מועד סבב טלפונים | Placement עם `round_number` (1..3); מספר הסבבים בפועל תלוי בימים הפעילים (דחיסה) |
| מי נכנס | `pending`/`maybe` + יש טלפון + אירוע עם `rsvp_track_active` + תאריך לא עבר |
| מי יוצא | אישר/ביטל (מיידי, דרך אותה שורת `Guest`) · ניסיון בסבב הנוכחי · callback עתידי · מספר שגוי · אירוע שעבר |
| סטטוסים | RSVP: `pending/confirmed/declined/maybe`. תוצאות שיחה: `confirmed/declined/no_answer/busy/wrong_number/callback` |
| שמירת שיחה | `CallLog`: event, guest, round_number, outcome, note, callback_at, phone_at_call, created_by, created_at |
| ניסיונות | כל `CallLog` = ניסיון. **אין מגבלת ניסיונות** — ניסיון אחד לסבב |
| הקצאה | `CallAssignment` — **ברמת אירוע בלבד** (טלפן ↔ אירוע). טלפן בלי הקצאות רואה הכול (התנהגות "שלב א'" מכוונת) |
| תוצאה → RSVP | `confirmed/declined` עוברים דרך `rsvp_response` — אותה פונקציה של WhatsApp ✅ |
| הסבב הבא | נגזר אוטומטית: הגיע תאריך הסבב הבא → המוזמן חוזר לתור |
| טווחי תצוגה | היום / מחר / בהמשך / לא טופל — מחושבים בהרצה עם `now` וירטואלי |

**מה טוב ושומרים עליו:** מנוע אחד ללוח הזמנים (אין שני לוחות), סנכרון מיידי עם RSVP,
סגירה עצמית של "מספר שגוי", 8 קבצי טסט ייעודיים.

### ב.2 פערים מול מה שביקשת

| # | דרישה | מצב | למה |
|---|---|---|---|
| 1 | **Timezone ישראל** | 🐞 **באג אמיתי, השפעה מוגבלת היום** | "היום" בתור השיחות ובלוח הזמנים מחושב כ-`datetime.utcnow().date()`. בין 00:00 ל-03:00 שעון ישראל המערכת עדיין חושבת שזה אתמול. בשיחות בשעות עבודה זה כמעט לא מורגש (ושליחת WhatsApp כבר משווה בשעון ישראל), אבל ברגע שיש בחירת תאריך, "אתמול" ורשומות משימות שמורות — זה חייב להיות מדויק |
| 2 | "אתמול — מה תוכנן מול מה טופל" | ❌ לא אפשרי | אין רשומה של "מה היה מתוכנן". מי שאישר היום נעלם מחישוב של אתמול |
| 3 | בחירת תאריך חופשי | ❌ | רק 4 טווחים קבועים |
| 4 | הקצאה למוזמן/משימה, העברה דני→יוסי | ❌ | הקצאה רק ברמת אירוע |
| 5 | סטטוס "הוקצה"/"בשיחה"/"דורש טיפול" | ❌ | אין ישות משימה שנושאת סטטוס |
| 6 | שינוי תאריך שיחה / ביטול משימה / Bulk | ❌ | אין מה לשנות — הכול מחושב |
| 7 | עצירה/השהיה/פתיחה מחדש של סבב | ❌ | |
| 8 | זמינות טלפן (חופשה, לא זמין היום) | ❌ | רק `disabled` |
| 9 | "דורש בדיקה" (אירוע בלי טלפן, סבב שלא הושלם...) | ❌ | |
| 10 | Timeline מאוחד WhatsApp + שיחות | ⚠️ חלקי | `guest_call_history` + `/automation/timeline/{guest}` קיימים בנפרד |
| 11 | ביצועים באלפי מוזמנים | ⚠️ | כל בקשה סורקת את כל האירועים הפעילים ומריצה את `build_queues` עד 3 פעמים; Pagination נעשה בזיכרון |
| 12 | יומן הקצאות | ⚠️ | רק `admin_caller_assignments` ברמת אירוע |

### ב.3 הארכיטקטורה המוצעת: "פנקס משימות" מעל לוח הזמנים הקיים

**העיקרון:** לוח הזמנים (`rsvp_timeline`) נשאר מקור האמת היחיד ל"מתי". מוסיפים שכבה
דקה שמתעדת **מה נקבע לכל מוזמן** — כי בלי תיעוד אי אפשר לענות על "מה היה אתמול",
להקצות מוזמן לטלפן, או להעביר משימה.

```
rsvp_timeline (ללא שינוי) ──► materialize_call_tasks(date)  ── idempotent
                                   │  UNIQUE(guest_id, event_cycle, round_number)
                                   ▼
                              call_tasks  ◄── CallLog (ניסיונות, קיים) 
                                   │
               ┌───────────────────┼─────────────────────┐
          עבר/היום:            עתיד (מחר/תאריך):        שינוי RSVP:
          רשומות שמורות       Preview מחושב חי          המשימה נסגרת אוטומטית
          (ניתן לשחזר)        (לא נשמר — מתעדכן          ("closed_by_rsvp") —
                              מעצמו כשהלוח משתנה)        לעולם לא נשארת בתור
```

- **`call_tasks`**: `event_id, guest_id, event_cycle, round_number, due_date (ישראל), reason,
  status, assignee_id, closed_reason, closed_at, created_at, updated_at`.
  UNIQUE על (מוזמן, מחזור אירוע, סבב) → **כפילויות נחסמות במסד**, לא רק ב-UI.
- **יצירה:** בכל פתיחה של מרכז השליטה לתאריך ≤ היום (אין cron ב-Render free; פעולה
  אידמפוטנטית, ולכן בטוחה להרצה חוזרת). אם בעתיד יתווסף cron — אותה פונקציה.
- **שינוי לוח זמנים / מועד סגירה (תרחישים 7–8):** משימות עתידיות לא קיימות כרשומות ולכן
  מתעדכנות מעצמן. משימה פתוחה שנוצרה כבר ושהסבב שלה זז — מתעדכנת ב-materialize הבא,
  **רק אם לא נגעו בה ידנית**.
- **סטטוסים (עקביים עם הקיים, בלי להמציא):**
  - סטטוס משימה: `open` (ממתין) · `done` (טופל) · `closed_by_rsvp` (אישר/ביטל ממקום אחר) · `cancelled` (בוטל ידנית) · `skipped` (האירוע עבר / מסלול נעצר)
  - "הוקצה" = `assignee_id` לא ריק (לא סטטוס נפרד)
  - התוצאה = `CallLog.outcome` הקיים; "לא ניתן להשיג" = `busy`; "מספר לא תקין" = `wrong_number`
  - "דורש טיפול" = **נגזר** (יותר מדי ניסיונות / callback שעבר / מספר שגוי) — לא סטטוס ידני
  - **לא מוסיפים "בשיחה"** — אין שום אות אמיתי שמישהו בשיחה כרגע; זה יהיה נתון מומצא
- **`caller_profiles`**: `user_id, availability (active/inactive/vacation), unavailable_dates, group, daily_capacity`.
- **הקצאה אוטומטית מאוזנת:** מתחשבת בהקצאה קיימת, callback לאותו טלפן שטיפל, זמינות
  וקיבולת. **תמיד מציגה תצוגה מקדימה ורק אחרי אישור מבצעת.** השרת דוחה הקצאה לטלפן לא פעיל.
- `CallAssignment` (ברמת אירוע) **נשאר** — הופך ל"טלפן ברירת מחדל לאירוע".

---

## חלק ג' — ארכיטקטורת ה-Admin החדש

### ג.1 מבנה ניווט

```
ראשי            ראשי
ניהול           משתמשים ואירועים · מאגר אולמות · אישורי הגעה · מרכז שליטה בטלפנים · בקשות דחייה
שליטה ב-VEYA    פיצ'רים והרשאות · כללי המערכת
מסחר            מסלולים ומחירים · תוספים · עמלות · קופונים והטבות · מנויים
מערכת           יומן פעילות · הגדרות Admin
```
- "קבלת מתנות" (Payout) → נכנס כ-Tab תחת **עמלות/כספים** או כ"דורש טיפול" בדשבורד.
- "הודעות ומסלול" → נבלע ב**כללי המערכת › אישורי הגעה › ספריית הודעות**.
- ניווט מבוסס hash (`#/admin/events/123`) — deep links בלי להוסיף ספריית router.

### ג.2 מודל נתונים מוצע (backward-compatible; אין מחיקת עמודות)

**עיקרון מפתח — Registry בקוד + ערכים ב-DB:** כל הגדרה/פיצ'ר מוגדרים בקוד (סוג, ולידציה,
ברירת מחדל = הערך הקבוע של היום). ה-DB שומר **רק שינויים**. כך: DB ריק = התנהגות זהה
להיום, ואי אפשר להזין ערך שהקוד לא יודע להפעיל.

| טבלה | תפקיד |
|---|---|
| `admin_role` (עמודה ב-users) | `super_admin / admin / support`. כל `is_admin` קיים → `super_admin` |
| `system_settings` | key, value JSON, updated_by, updated_at |
| `setting_overrides` | scope (`plan/user/event`), scope_id, key, value, reason, created_by |
| `features` | key, status (`active/beta/off`), description — רישום פיצ'ר חדש בלי קוד (לגייטינג) |
| `feature_rules` | feature_key, scope (`all/plan/user/event`), scope_id, enabled, limits JSON |
| `plans` / `plan_versions` | גרסה בלתי-משתנה: מחיר, מגבלות, פיצ'רים. שינוי מחיר = גרסה חדשה |
| `addons` | key, name, price, status, standalone, included_in (plan ids) |
| `fee_rules` | סוג (`gift_card`), basis_points, fixed_agorot, direction, min, max, scope |
| `coupons` / `coupon_redemptions` | |
| `event_entitlements` | אירוע ↔ plan_version נעול + addons + סטטוס (ראו שאלה 2) |
| `venues` (+עמודות) + `venue_images` | CMS; תמונות דרך `media_blobs` הקיים |
| `admin_audit_logs` | actor, role, domain, action, target, **before/after JSON**, reason, ip, created_at |
| `call_tasks` + `caller_profiles` | חלק ב' |

**שרשרת הערכים:** `resolve(key, event)` → Event Override › User › Plan version › System setting › ברירת מחדל בקוד.
מחזיר **ערך + מקור**, כדי שה-UI יציג: "2 סבבים · Override לאירוע · ברירת המערכת: 3 · [איפוס]".

**עמלה:** היום `int` אחוזים (4). "3.5%" דורש מעבר ל-basis points (350). הכיוון —
**מתווספת על הנותן** — נשאר נעול; האדמין יוכל לשנות את הגובה, לא את הכיוון, בלי אישור מפורש.

### ג.3 אבטחה

- `require_admin_permission("commerce.fees.write")` — dependency חדש בשרת, מעל `get_current_admin`
  הקיים (שממשיך לעבוד כפי שהוא).
- מטריצת הרשאות בקוד (`admin_permissions.py`) — Support: משתמשים/אירועים/טלפנים (קריאה + פעולות תמיכה);
  Admin: + הגדרות ואולמות; Super Admin: + מסחר, מחיקות, עצירת חירום, ניהול אדמינים.
- פעולות מסוכנות: confirmation בשרת (`confirm_text` חייב להתאים) + חובת רישום ביומן באותה טרנזקציה.
- RLS: כל הטבלאות החדשות — `app_is_admin()` בלבד; `call_tasks` — גם לטלפן, רק שורות שהוקצו לו / אירועים שלו.
- מניעת הסלמה: אדמין לא יכול להעניק דרגה גבוהה משלו או לשנות את עצמו; תמיד נשאר Super Admin אחד לפחות.
- Impersonation כבר קיים — יוגבל להרשאה מפורשת + סיבה חובה ביומן.

### ג.4 מה **לא** נבנה (כדי לא לזייף)

- כפתורי חיוב/חידוש/החזר כספי — אין ספק סליקה. יוצג "מתוכנן", מושבת, עם הסבר.
- "WhatsApp עובד כרגע" — יוצג רק מה שידוע באמת: mode (mock/live), אחוז כשלון בשליחות אחרונות. לא "online".
- הקצאת "בשיחה עכשיו" — אין אות אמיתי.
- מצב "אלבום תמונות" — הפיצ'ר לא קיים בקוד; ניתן לרשום אותו כ-Feature כבוי לתכנון בלבד.

---

## חלק ד' — סדר ביצוע מוצע

שיניתי את הסדר בשתי נקודות, עם הסבר:

| Phase | תוכן | למה בסדר הזה |
|---|---|---|
| **2** | **תשתית אבטחה**: דרגות אדמין, `admin_audit_logs`, dependency הרשאות, תיקון Timezone | כל שלב אחריו מוסיף כפתורים מסוכנים — קודם המנעול, אחר כך הכפתורים |
| **3** | מעטפת + Sidebar + דשבורד "מה צריך עכשיו" + Global Search + Alerts | |
| **4** | **מרכז שליטה בטלפנים** (call_tasks, זמינות, הקצאה, תאריכים, חריגות, 8 תרחישי הבדיקה) | הגדרת אותו כקריטי; הוא גם עובד על הנתונים הכי חיים |
| **5** | משתמשים ואירועים + Event Control Center | |
| **6** | פיצ'רים והרשאות + כללי המערכת + Overrides (כולל הפיכת `CYCLE` לניתן להגדרה, ברירות מחדל זהות להיום) | הכי רגיש ל-RSVP/WhatsApp — אחרי שיש יומן ובדיקות |
| **7** | מאגר אולמות CMS | |
| **8** | מסחר (מסלולים+גרסאות, תוספים, עמלות, קופונים, זכאויות) | |
| **9** | מעבר UX, מובייל, Empty/Loading/Error, ניקוי | |

בסוף כל Phase: pytest (מול הבסיס 637), `tsc`, בדיקה בדפדפן, קומיט.


---

## חלק ה' — מצב ביצוע (מתעדכן)

| Phase | מצב | מה נבנה בפועל |
|---|---|---|
| 1 | ✅ | המסמך הזה |
| 2 | ✅ | `admin_rbac.py` (3 דרגות, נאכף בשרת על כל נתיבי האדמין), `admin_audit_logs` + RLS 19, `local_time.py` ("היום" בשעון ישראל), חסימת הסלמת הרשאות |
| 3 | ✅ | `src/admin/` — מעטפת, Sidebar לפי הרשאות, ניווט hash, דשבורד "מה צריך עכשיו" (`admin_overview.py`), חיפוש גלובלי, יומן פעילות, הגדרות Admin |
| 4 | ✅ | `call_ops.py` + `routers/call_ops.py` + RLS 20 — פנקס משימות מעל לוח הזמנים, מרכז שליטה בטלפנים, ניהול טלפנים וזמינות, `/my` לטלפן, 12 בדיקות לתרחישים |
| 5 | ✅ | `routers/admin_people.py` — משתמשים/אירועים עם סינון ודפדוף בשרת, מרכז שליטה לאירוע, כניסה לאירוע לתמיכה |
| 6 | ✅ | `settings_registry.py`, `features.py`, `routers/admin_rules.py`, RLS 21 — כללי המערכת, Overrides לאירוע, פיצ'רים וחריגות, עצירת חירום; `routers/admin_rsvp.py` — מסך אישורי הגעה תפעולי |
| 7 | ✅ | `routers/admin_venues.py` + RLS 22 — CMS אולמות: פרטים, סוגי אירוע, קיבולת, תמונות (סדר/ראשית), מידע פנימי, אימות, עדיפות, סטטוס פעיל/מוסתר/טיוטה, שכפול, מחיקה בהקלדת שם. טיוטה/מוסתר לא מוצעים לזוגות; כתובת של אולם מאומת לא נדרסת |
| 8 | ✅ | `commerce.py`, `routers/admin_commerce.py` + RLS 23 — מסלולים בגרסאות, תוספים, עמלת אשראי נשלטת (בלי כלל = 4%), קופונים, מסלול לאירוע (פותח פיצ'רים) |
| 9 | ✅ | ניקוי מסכים ישנים, הדשבורד וטלפנים מאותו מקור, חיפוש גלובלי גם בפיצ'רים ומסלולים, בדיקת מובייל ל-17 מסכים (בלי גלילה אופקית) |
| איחוד | ✅ | אחרי ה-Audit (2026-09-22): `call_tasks` מקור אמת יחיד לתור — ראו "איחוד מרכז הטלפנים" למטה |

### איחוד מרכז הטלפנים (2026-09-22)
זרימה אחת: אירוע → לוח RSVP → אורח זכאי → סבב → **משימה** → טלפן משויך → שיחה → תוצאה → הפעולה הבאה.
- **מסך הטלפן** (`PhoneAgentApp.tsx`) קורא רק `/admin/call-ops/my/*`: `my/day`, `my/tasks?group=work|later`, `my/tasks/{id}`, `my/tasks/{id}/outcome`. רואה רק משימות עם `assignee_id` שלו (שרת + RLS 24). לא מחשב תור.
- **תיעוד שיחה** — פונקציה אחת: `call_ops.record_call`. גם `/admin/call-center/.../outcome` הישן (לתאימות לאפליקציות שמורות) מאתר משימה (`task_for_guest_now`) וקורא לה. תוצאות: מגיע / לא מגיע / עדיין לא בטוח / ענה בלי החלטה / לא ענה / לא זמין / מספר שגוי / שיחה חוזרת / הערה בלבד (לא נספרת כניסיון).
- **`CallLog` = היסטוריה בלבד**, עם `event_cycle` + `task_id` + `round_number` מהמשימה (לא ניחוש מתאריך). שיחות ישנות קיבלו מחזור ב-`_migrate_call_log_cycles`.
- **דחייה:** `postponement_service.complete` סוגר את משימות המחזור הישן (`cycle_closed`), לא מוחק. התור, הספירות והתראות "מספר שגוי" — רק מהמחזור הפעיל. ההיסטוריה של מחזור קודם מוצגת לאדמין מסומנת.
- **שיחה חוזרת:** `call_tasks.callback_at` — חובה שעה; לא מופיעה ב"לחייג עכשיו" לפני הזמן (`group=later`), לא נמחקת ב-sync.
- **טלפון:** זכאות לפי `invitations.classify_phone` בלבד (`call_ops.phone_ok`). מספר שתוקן חוזר לתור.
- **sync:** `INSERT … ON CONFLICT DO NOTHING` על (guest, cycle, round) + נעילת advisory ב-Postgres. חוזר/מקבילי = אותה תוצאה.
- **סנכרון יומי:** `POST /internal/jobs/call-sync` (`routers/jobs.py`) — פעיל רק כשיש `VEYA_JOB_SECRET` (24+ תווים) ובכותרת `X-Veya-Job-Secret`, אחרת 404. מופעל ע"י `.github/workflows/call-sync.yml` (00:05 ו-07:30 שעון ישראל). דורש Secrets ב-GitHub: `VEYA_API_URL`, `VEYA_JOB_SECRET`; ובשרת Render אותו `VEYA_JOB_SECRET`.
- בדיקות: `tests/test_call_unified.py` (10).

### מה מחובר באמת (שינוי במסך משנה התנהגות)
- מספר תזכורות WhatsApp, מספר סבבי טלפונים, אורך התהליך, סגירת רשימה לאירוע קרוב — `rsvp_timeline.policy_for` (מערכת + Override לאירוע)
- עצירת חירום — `messaging.get_provider` מחזיר ספק שלא שולח
- סף "דורש טיפול" בטלפנים — `call_ops.many_attempts`
- פיצ'רים: מתנות באשראי (פותר בזכאות, קדימות מעל מתג הסביבה), טלפנים לאירוע (sync מדלג), זיהוי סקיצה (מתג כללי)

### מה מוצג לקריאה בלבד, עם הסבר (לא כפתור מזויף)
- מצב WhatsApp mock/live (משתנה סביבה), חסימת שישי-שבת (כלל קבוע), שעת שליחה (לכל אירוע), ניסיונות לסבב, כיוון העמלה (החלטה נעולה)
- פיצ'רים שאין להם מתג בקוד: אישורי הגעה, WhatsApp, הושבה, כספי האירוע

### ידוע ופתוח
- **סנכרון יומי קיים** (ראו "איחוד מרכז הטלפנים") — אבל פעיל רק אחרי הגדרת `VEYA_JOB_SECRET` ב-Render וב-GitHub. עד אז משימות נוצרות כשמישהו פותח את הדשבורד/מרכז הטלפנים.
- ה-endpoint של הסנכרון היומי רץ עם `MigrationSessionLocal`; כש-RLS נאכף, `MIGRATIONS_DATABASE_URL` חייב להיות תפקיד מורשה (אותה דרישה כמו מיגרציות העלייה).
- שינוי מספר הסבבים באירוע שהמסלול שלו כבר פעיל ממספר מחדש את הסבבים מהיום (מוצגת אזהרה).

- **סביבת הבדיקות כותבת ל-`backend/veya.db` המקומי** (בעיה קיימת מראש: `app.database` נטען לפני ש-`make_client` קובע DATABASE_URL). לכן המסד המקומי מכיל ~19 אלף משתמשי בדיקה. לא נוגע בייצור.
- מסחר: אין סליקה. מסלול לאירוע פותח פיצ'רים; מגבלת מוזמנים ומימוש קופון עוד לא נאכפים (כתוב במסכים).
- `venues` בלי RLS (החלטה קיימת: קטלוג ציבורי) — עכשיו יש בו גם שדות פנימיים (איש קשר, הערות). ה-API הציבורי מחזיר רק שם וכתובת; אם יש חשיפה ישירה של הטבלה ב-Supabase (anon) — לבדוק.
- פריסה: קובצי RLS 19–23 רצים אוטומטית בעליית השרת (`_RLS_MIGRATION_FILES`); עמודות חדשות — `_EXTRA_COLUMNS`.
